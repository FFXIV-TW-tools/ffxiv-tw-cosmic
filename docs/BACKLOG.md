# BACKLOG — ffxiv-tw-cosmic

> 排序即優先序（上=先做）。條目由 agent 提、Owner 排序/否決；Owner 也可直接加。**不經 Owner 核可不得自主實作**。
> 完成打勾並附 cycle；否決用刪除線並留一行原因。格式見 DEVLOOP §4.2。

- [x] **B-025** (P2, perf)【建議 高｜延遲風險 低｜執行風險 低（純新增 `<link>`，不動邏輯、不改模組求值順序）｜副作用 index.html 多 ~1.3KB；新增／刪除 module 後須重跑生成器，哨兵會擋】**展平 ES module 載入瀑布** — 接同日「各伺服器現況」那則：那則拆的是串行鏈**最後一段**，這則拆**中間那段**（`app.js → 20 支 module` 本身也是逐層發現）。同型病灶在 market 量到 production 排成 5 波（0→300→360→420→540ms），同一批檔平行抓只要 150ms。加 19 條 `<link rel="modulepreload">` 讓瀏覽器解析 HTML 時就一次抓完。⚠️ 清單**生成不手貼**（漂掉只會「慢一點」而無任何錯誤訊號）；生成器與哨兵是跨站樣板、**各站只准差 `ENTRY` 一行**。來源: Owner 指示 2026-08-05 ✓ 完成於 cycle 2026-08-05-B-025（`tools/gen-modulepreload.mjs` ＋ 哨兵 `tests/modulepreload-drift.test.mjs`，後者與 market 版逐字節相同；run-all 2→3 檔）

- [x] **B-024** (P2, feature)【建議 高｜延遲風險 中（每次事件都在流失最可能出手的那群人）｜執行風險 中｜副作用 fan-out 的 webhook URL 會多帶一個 query 參數；Discord 若不吃 components 需走 fallback】**緊急事件 Discord 通知補網址＋附議／否認入口** — Owner 2026-08-05 提出。現況 `logic.js:409 discordPayload()` 只有標題＋一句倒數、**零連結**：收到通知的人要附議得自己找書籤回站、切分頁、找卡片。而附議正是這功能的社交層命脈（可信度靠附議、下架靠否認）⇒ 等於把「人正在看 Discord、正在遊戲裡」的最佳出手時機擋在流程外。修法＝`embed.url` ＋ 兩顆投票入口。⚠️ **真互動按鈕做不到**：使用者自建的 incoming webhook 是 non-application-owned，Discord 明文不允許送互動元件，且按下去的 interaction 沒有端點收得到（要有 bot + 公開 endpoint + 簽章驗證）；可行路線是 **link button（`style:5`，官方明文「不送 interaction」）配 `?with_components=true`**——但這句是**由兩段文件推導、未實測**，Build 第一步就是拿測試 webhook `curl` 一發定 A/B（B＝markdown 連結行）。⚠️ 點連結**不自動投票**（Discord 會預抓 preview ⇒ 幽靈票；否認的 `disputed` 是單向不可回頭；新裝置＝全新 uuid），落在事件卡上就地確認才送。spec `docs/specs/2026-08-05-B-024-emergency-discord-deeplink-design.md`（**draft，§7 兩個決策點待 Gate 1**）。來源: Owner 2026-08-05 ✓ **完成於 2026-08-05**（cycle `2026-08-05-B-024-emergency-discord-deeplink`）：實測六發定案 **Components V2 Container**（按鈕包在色框內，Owner 看真機截圖後拍板）；預告那一則**也給投票**（Owner 修正原設計——預兆通告本身就是遊戲內看得到的東西，附議有明確指涉，且同一個 event id 會沿用到開始那則）。⚠️ **做不到的三件事已實測釘死**：彩色按鈕 400（彩色 style 要 `custom_id`＝互動元件，非 app webhook 禁送）／`label` 不能上色／外連圖示 ⧉ 無欄位可關。送出必帶 `?with_components=true`（不帶＝按鈕被靜默忽略、訊息照樣 204）。V2 對非 app webhook 無官方保證 ⇒ 400 時退回純 embed 重送且不計失敗（`fanout_v2_reject` 分桶）。測試 27→**34** 純函式、56→**60** 整合，六種回流注入皆驗過轉紅。**收官於 2026-08-05**：Pages 與 worker 皆已部署（worker version `ddec1859`），前端收端在**正式站＋真事件 `id=73`** 實測通過，三則真 payload（進行中／預告／退回版）由部署中的 `logic.js` 產生並實際送達 Owner webhook。⚠️ **仍未覆蓋**：線上 DO 的 fan-out 路徑（訂閱查詢→併發送出→400 退回）沒有端到端實測——要驗它必須在站上真通報一筆，那會讓所有訂閱該伺服器的人收到假警報，屬對外行為故不自行觸發；退回路徑本身由 `http.test.ts` 的整合測試守（含 signal 隔離與 `fanout_v2_reject` 分桶）。
- [x] **B-003** (P1, chore) — ✓ **早已完成，條目過期未關（2026-08-02 實測補記）**：`git remote` 指向 `FFXIV-TW-tools/ffxiv-tw-cosmic`、`https://ffxiv-tw-cosmic.pages.dev/` 回 200 且內容為本站（三個分頁齊全）。**部署面同時複驗無外洩**：`AGENTS.md`／`docs/BACKLOG.md`／`worker/src/index.js` 雖回 200，但 `Content-Type: text/html` 且 body 是 index.html ⇒ CF Pages 的 SPA fallback，不是實際檔案（對照組：不存在的 `nonexistent-xyz.md` 回應完全相同）。允許清單有生效。**教訓：光看狀態碼會誤判成外洩，必須看 content-type 與 body**（`tools/check-deploy-surface.sh` 正是為此而分辨）。原條目：【建議 高｜延遲風險 中｜執行風險 中｜副作用 觸發 CF Pages 首次部署】【本機準備已完成 2026-08-01：deploy 三件組就緒、portal 三處已註冊（tools.json／_middleware ALLOWED／header.js FALLBACK）、hooks README 涵蓋清單已補、BETA 標示與待驗證告示已上。**剩下必須由 Owner 執行**：① `git config core.hooksPath`（hook 硬擋 agent）② `gh repo create` ③ CF Pages 連接 ④ portal push ⑤ GSC 提交】【建議 高｜延遲風險 中｜執行風險 中｜副作用 觸發 CF Pages 首次部署】上線部署（見 `../_NEW-TOOL.md` step 2–5）：`gh repo create FFXIV-TW-tools/ffxiv-tw-cosmic` + CF Pages 連接 + portal `tools.json` 註冊（icon 🌙／accent cyan／category daily）+ `functions/_middleware.js` 的貓小胖白名單 + `header.js` 的 `FALLBACK_TOOLS` + `tools/git-hooks/README.md` 涵蓋清單 + GSC 提交 sitemap。不做的後果：站做好但沒有入口、也不會被搜到。來源: 2026-07-31-cosmic-site

- [x] **B-004** (P2, chore) 遊戲內核對「ET 時段條件」的單位與語意 — **已驗證通過**（2026-07-31，cycle 2026-07-31-cosmic-taxonomy）。Owner 遊戲內回報 #445「採集精密空氣淨化裝置所需的材料」（園藝師／A）為 ET 10:00~11:59，本站解出 10:00–12:00 逐位吻合；同名的採掘師版 #400 是 02:00–04:00，兩者不同 ⇒ 排除「碰巧對上任意 2 小時級距」。單位確認為艾奧傑亞小時，`window-index.js` 的 `verified` 已改為時段與天候同列。

- [ ] **B-016** (P2, chore)【建議 中｜延遲風險 中｜執行風險 低｜副作用 若成立，面板要把同組合併成一列並標明「擇一」】驗證 **同名跨職組是否擇一／輪流出現**（Owner 2026-07-31 假設）。資料上這種組確實存在且有共用鍵：同名（去【高難】/【高難+】前綴）的任務**共用同一個 `c1`**，只有職業與需求物不同——例 #74/#119/#164「【高難】補充月門基地的備用品」＝鍛造／甲冑／金工，需求物是棒狀／室外／室內照明。全表 **40 組三筆、48 組兩筆、293 筆單獨**。若「同組擇一」成立，某職業看到該任務的機率就只有 1/組員數，這正好能解釋 Owner 多次「條件成立卻沒看到」的觀察，也與條件表原名 `Lottery` 一致。**驗法**＝在同一個條件窗內把該組三個職業各切一次看板，記錄每次出現的是哪一個；重複 5 窗以上。若每窗只出現一個且輪替 ⇒ 成立。目前只在該列的 hover 說明列出「同名任務另有：甲冑師、金工師」，**不拿它去過濾或推算**。來源: Owner 2026-07-31 假設

- [x] **B-015** (P1, chore) **已定案（靠實測，不是推理）：`c14` 非零＝該任務目前整批拿不到，語意未知；本站標 `blocked` 不預測。** ICE `BoardLogger` 實測（2026-07-31 園藝師 23 筆）：實際出現過的任務**全部 c14=0**（7 次出現零例外）；c14 非零者在 **208 次機會中出現 0 次**，包含 32 次「c14 自己的視窗也開著」⇒ 不只是多一道時段閘。值分布 1:59 2:35 3–10 各 1 13:11 16:6，像分階解鎖但對不上 16 期開發階段。**下一步**＝多收幾個職業與幾天的記錄，看是否有任何 c14 非零的任務曾出現；若始終為 0，再查它對應哪個解鎖軸。舊結論（下方）建立在「SinusMapV2 與 c15 22/22 吻合」上，那只證明 c15 是條件之一、**不證明 c14 不是**——吻合度高的旁證只能加分，不能排除另一個變因。原條目： 決定性證據＝ICE fork 內建的手工對照表 `PlayerHandlers.SinusMapV2`（上游依遊戲內觀察建的「ET 時段 → 任務」表）與 c15 **22/22 全吻合**，且每一筆雙槽任務都被列在 c15 的時段、零筆對到 c14（例 #448 列在 ET 22–24＝c15，不是 c14 的 ET 00–02）。原條目：【建議 高｜延遲風險 高｜執行風險 中｜副作用 若成立，主面板要改成只吃 c15，條件任務從 152 掉到 63】**c14 可能根本不是條件欄**。兩項獨立訊號同時指向這件事：

  ① **Owner 已確認「看得到」的三筆全部 `c14=0`，「看不到」的三筆全部 `c14≠0`**（看得到 #76／#437／#445，看不到 #74／#448／#506）。以 c14≠0 佔 119/544 估，這個 3/3 分裂約 0.5% 機率是巧合。

  ② **分布形狀**：只算 c14 → ET 00-02 有 59 筆、02-04 有 35 筆、其餘每窗 1 筆、**20-22 與 22-24 完全空白**、天候只有靈風沒有月塵；只算 c15 → 12 個 ET 窗每窗 1–3 筆、靈風 20／月塵 21，**完整且均勻**。c15 長得像真的條件表，c14 不像。這同時解釋了 B-010 的「極端集中」——那可能是誤讀 c14 的產物。

  **但不能直接改**：三個「看不到」全是**單次缺席**，而條件表原名帶 `Lottery`（抽選）⇒ 任何單次缺席都能被抽選解釋，證據力遠低於「看得到」。反過來 c15-only 也有反例（#448 c15=12 在 ET 23:00 沒出現、#448 若只看 c15 該出現）。**驗法（唯一能定案的）＝挑一筆 `c14≠0, c15=0` 的任務（如 #74，ET 02:00–04:00），連續盯 5 個以上該窗；全都沒出現＝c14 不是條件。** 反向再挑一筆 `c14=0, c15≠0` 的做對照。來源: Owner 2026-07-31 連續實測

- [x] **B-010** (P1, chore) **已解釋：** ET 分布極端集中（00-02 佔 62 筆）是誤讀 c14 的產物。改讀 c15 後 12 個時段各 1–3 筆、合計 22，與 ICE 手工表逐筆吻合。原條目：【建議 高｜延遲風險 中｜執行風險 低｜副作用 可能推翻 ET 條件的整體解讀】遊戲內核對「ET 時段條件的分布為何極度集中」。124 個時段任務裡 **ET 00:00–02:00 佔 62 個、02:00–04:00 佔 36 個**，其餘 10 個時段加起來只有 26 個（每段 1–4 個）。後果＝一天有大半時間看板上「必定出現」真的是 0，Owner 2026-07-31 因此質疑「怎麼可能所有職業一個都沒有」。**單筆映射已驗證正確**（#445 園藝師/A ＝ ET 10:00–12:00，遊戲顯示 10:00~11:59），所以不是列號對錯位；也已排除「c14 只是旗標」——c14 的值域（1–10、13、16）與 cond row id 完全相同。**驗法**＝在 ET 00:00–02:00 進遊戲看任務板，確認是否真的一次湧出數十個時段任務；若不是，代表 c14/c15 其中一欄還有第二層語意（例如「起始窗」＋「重複間隔」）。來源: Owner 2026-07-31 質疑

- [x] **B-011** (P2, chore) 雙條件槽的邏輯 — **定案為純 AND**（2026-07-31，被推翻三次後收斂）。最後一次否證＝Owner 在 **ET 23:00 用園藝師看不到** #448「【高難+】傳承錄級材料的採集調查」（ET 00:00–02:00 ＋ ET 22:00–24:00），OR 之下它該在板上 ⇒ 同類型也不是 OR。先前的證據：靈風時段中篩鍛造＋園藝，遊戲內只有 2 個限時任務，本站當時（OR）列了 8 個；扣掉 3 個緊急任務後，多出的 3 個非緊急任務全是 ET＋天候雙條件（用岩盤挖掘機開路 ET 04–06＋靈風、月底的精密調查 ET 02–04＋靈風、製造高火力爐 ET 12–14＋靈風），靈風確實開著卻都不在板上 ⇒ OR 被否證。19 筆屬此類。

- [x] **B-012** (P2, chore) **已消滅：** 「兩槽皆 ET 互斥」是把 c14 誤當條件的產物，改讀 c15 後每個任務最多一個條件，不存在互斥。原條目：【建議 中｜延遲風險 低｜執行風險 低｜副作用 定性後要決定放不放回主面板】定性 11 筆「兩槽皆 ET 時段」的任務。純 AND（已定案）之下它們**在任何時刻都不可滿足**，Owner 2026-07-31 的 ET 23:00 實測也確實看不到。形態極規則：**每個職業剛好一筆、全是【高難+】、10 筆的第一槽都是 ET 00:00–02:00**，第二槽逐職不同（木工 12–14／鍛造 16–18／甲冑 20–22／金工 00–02（與第一槽相同）／皮革 04–06／裁縫 08–10／鍊金 12–14／烹調 16–18／採掘 14–16／園藝 22–24／漁 02–04＋18–20）。而且它們是各職連續任務鏈的**鏈頭**（如 #448 → #449 → #450），鏈頭不可達等於整條鏈是死內容——這不太像實裝內容該有的樣子，比較可能是 c15 對這組另有語意。**目前一律判「無法判定」、主面板不列，也不宣稱它們永遠不可能。** 驗法＝在 ET 00:00–02:00（兩槽的共同前綴）用對應職業看板上有沒有；若有，代表第二槽根本不是條件。來源: Owner 2026-07-31 實測

- [ ] **B-013** (P2, chore)【建議 中｜延遲風險 低｜執行風險 低｜副作用 目前已從主面板排除，定性後要決定放不放回去】定性**雙職業任務**（16 筆）為什麼條件成立也不出現。實證：Owner 2026-07-31 在 ET 12:00–14:00 且靈風時，本站列出「【高難+】製造高火力爐」（園藝＋甲冑）但**遊戲內沒有**，而同一畫面的兩個單職業任務（對材料進行耐磨加工／採集輔助加工的材料）確實在板上。**「第二職沒練」的解釋已被 Owner 本人否證**（兩個職業都有，仍看不到）。資料上它們自成一區：`JobCategorySecondary` 非零者恰好等於 `c21 ∈ {20000, 20100}` 那 16 筆（交集 16/16，id 連續 #496–511），而且是全表唯一沒有 rank-4 對應版本的一組（其餘 11 組都是 X000/X100/X200＝高難+/高難/A 各 8 筆）。**剩下的候選解釋**：① 第二職的宇宙探索階級（不是等級）也要到同階 ② 需要組隊／協力 ③ 綁月門基地開發進度（Owner 目前第三期）。驗法＝挑一筆第二職宇宙階級也到位的，在條件成立時看板上有沒有。來源: Owner 2026-07-31 實測

- [ ] **B-014** (P1, feature)【建議 高｜延遲風險 中｜執行風險 低｜副作用 需要一個「現在可能不準」的狀態，且無法自動判定】緊急事件會**覆蓋天候**，本站的天氣預報在那段時間是錯的。Owner 2026-07-31 實測：靈風進行中突然轉成孢子霧、限時任務整批消失、改出緊急事件。孢子霧／磁暴／流星雨不在任何 `WeatherRate` 內（已證實）⇒ 純時間演算法永遠擲不出來 ⇒ **無法離線預測，也無法離線偵測它正在發生**。目前只在面板底部用文字告誡。可考慮的改善：① 讓使用者手動按一個「現在是緊急事件」開關，暫時把天氣限定任務標為不可用 ② 若日後接了插件回報（B-006）就能自動化。來源: Owner 2026-07-31 實測

- [ ] **B-008** (P2, chore)【建議 中｜延遲風險 低｜執行風險 低｜副作用 無】把 `WKSMissionUnit` c16＝前置任務（LockedBehind）回饋給 ICE fork。ICE 目前硬填 `previousMissionId = {0}`（`ICEDictornaryCreation.cs` 註解寫「候選 col[12]/col[16] 尚未定性」），現已定性為 **c16**：88 筆非零、指向同職業 88/88、名稱帶「續·」。ICE 拿到後才能正確判斷連續任務是否解鎖。屬 `XIVpluginsDev` 範圍（編號 `P-NNN`），本條只作交叉索引。來源: 2026-07-31-cosmic-taxonomy

- [x] **B-009** (P3, chore) **已否證：「條件成立＝必定出現」是錯的。** ICE 實測：#437 採集輔助加工的材料在條件全部成立的 12 次機會中只出現 5 次（**42%**）。條件表原名帶 `Lottery`＝條件成立只是取得抽選資格。全站措辭已改為「條件符合」，並在面板標明出現率約四成。原條目： (P3, chore)【建議 低｜延遲風險 低｜執行風險 低｜副作用 無】核對「條件成立＝必定出現」這條規則。看板目前據此把任務拆成「必定出現」與「抽選池」兩組，依據是 Owner 2026-07-31 的遊戲內觀察（天氣任務在該天氣期間一定在板上），**client 內沒有任何欄位能證明**。驗法＝靈風視窗開啟時看板上是否 22 個高難+ 天候任務全在。若出現反例（條件開了卻沒出現），`job-board-view.js` 的兩組拆法與 index.html 的定調文案要一起改。來源: 2026-07-31-cosmic-taxonomy

- [ ] **B-001** (P2, chore)【建議 中｜延遲風險 低｜執行風險 低｜副作用 修正別的 repo 的既有錯誤】修 `ffxiv-tw-sightseeing/modules/weather.js` 的**月夜峰（Mare Lamentorum）機率表錯誤**：該手打表寫 `[[15,"Umbral Wind"],[30,"Moon Dust"],[60,"Fair Skies"],[100,"Clear Skies"]]`（＝Fair 30 ＋ Clear 40），但台服 client（territory 959 → `WeatherRate#135`）是**靈風 15／月塵 15／晴朗 70，沒有碧空**。影響＝探索筆記若有月夜峰的天氣條件點位，預測會錯。順帶評估把「天氣演算法 + client 產生的機率表」升格到 portal 共用層（rule of two：本站與 sightseeing 各一份），需 Owner Gate1。來源: 2026-07-31-cosmic-site 勘查

- [ ] **B-002** (P3, chore)【建議 低｜延遲風險 低｜執行風險 低｜副作用 動 portal 共用 CSS】提報 portal 把 `.codex-empty[hidden]` 收進集中 `[hidden]` 守衛。`.codex-empty` 設了 `display:flex`，會蓋過 UA 的 `[hidden]{display:none}` ⇒ 帶 `hidden` 屬性的空狀態照樣顯示（本站踩到，已用工具自有 class `.cos-hideable[hidden]` 本地繞過）。header.css 現有守衛涵蓋 btn／chip／icon-btn／modal-overlay／help-pop，**就是漏了 empty**。需 Owner Gate1。來源: 2026-07-31-cosmic-site 實作

- [x] ~~**B-005** (P3, chore)【建議 低｜延遲風險 低｜執行風險 低｜副作用 無】定性 `WKSMissionToDoEvalutionRefin` 三欄（值如 20/40/70、50/60/85）。與 `WKSMissionUnit` 的銀星／金星（絕對分數）不同量綱，疑為銅/銀/金百分比門檻。目前落在 `_unverified.evalThresholds`、不進 UI。~~ — ✓ **定性完成於 cycle 2026-08-01-recipe-quality-stages**：官方欄名就是 `LowPercent`/`MidPercent`/`HighPercent`（global datamine `en_WKSMissionToDoEvalutionRefin.csv`）＝**滿品質百分比**，百分比推測正確。但同時發現本站的 join 是錯的（拿任務 id 當列號，真鍵是配方的 `Recipe.CollectableMetadata`）。整件事已移出本站 → monorepo `game_ref.sqlite` 的 `recipe_quality_stages`，消費端 crafter。**ICE fork 的「銅星門檻」線索仍有效**：`TcCapability` 要找的若是 per-recipe 門檻，就在這張表，鍵同樣是 `CollectableMetadata` 而非任務 id。來源: 2026-07-31-cosmic-site

- [x] **B-017** (P2, data) — ✓ **2026-08-06 以另一條路完成**（`feat(cosmic): 有配方的任務全部可求解`）：40 筆（24 緊急＋16 雙職業）的需求物改由**配方產出**（`Recipe.c4`）補上，依據＝360 個「兩者都有」的任務裡 需求物 360/360 落在該任務配方的產出集合內。抓不到需求物的任務 117 → **77**。⚠️ **原條目的修法（「補齊 `WKSMissionUnit` 的多個 ToDo 槽」）已證偽**：逐欄檢定過，除 col[11] 外唯一像 ToDo FK 的是 col[12]（恰 33 筆＝全部緊急任務、值＝col[11]+1），而那些列同樣沒有需求物。本條在 2026-08-08 健檢被抓到「open 條目指向已證偽的修法、數字也停在 117」才關掉。原條目：【建議 中｜延遲風險 低｜執行風險 中｜副作用 改 dump 欄位對照】補齊需求物：`WKSMissionUnit` 的多個 ToDo 槽。現況 **117 個任務抓不到需求物**（其中 40 個有配方、含全部 16 個雙職業任務 #496–511），因為 `TcMissionUnit.MissionToDo` 只讀 col[11] 一個槽。上游 BestCraft 的 entity 是 `ToDo0Id`/`ToDo1Id`/`ToDo2Id` 三個 ⇒ 台服對應欄位待反解。**影響**：那 40 個任務的「需求物」欄空白、連不到求解器。數字已寫死進 `validate.mjs`（修好會讓該條紅，記得把數字改小）。⚠ 反解時**先找已知能動的參考實作再動手**（AGENTS §10 精神）——這正是 `col[14]/col[15]` 與本輪 refin join 兩次踩過的同一種坑。來源: 2026-08-01-recipe-quality-stages

- [ ] **B-018** (P2, chore)【建議 中｜延遲風險 低｜執行風險 低｜副作用 無】上線後觀察通報品質，決定要不要收緊社交層防線。`GET /admin/stats` 已分桶（`report_ok_plugin`／`report_ok_manual`／`report_dup`／`vote_confirm`／`vote_dispute`／`event_disputed`／`fanout_fail`／`sub_broken`）。**要看的三件事**：① 手動通報的否認率——高就代表社交層不夠，要考慮「新 UUID 需 2 人附議」② 插件與手動對同一起事件的一致性——插件先報、手動後報＝手動可信；反過來就要查 ③ `fanout_fail`／`sub_broken` 是否集中在少數人（webhook 失效）還是普遍（我們的送法有問題）。**沒有可驗證的閘是這個功能的已知上限**（天氣閘已於 2026-08-02 被 ICE log 證偽），所以只能靠事後觀測。來源: 2026-08-02-emergency-report

- [ ] **B-019** (P1, fix)【建議 高｜延遲風險 中｜執行風險 低｜副作用 動投票與通知路徑】修後閘查出的四個正確性缺陷（2026-08-03 codex 後閘，每條都已實際讀程式碼驗證）：① **通報者可替自己的事件附議** ⇒ `confirms.size === 0` 永遠不成立 ⇒ 該筆再也不可能被 3 個否認下架（`logic.js` 的 `applyVote()` 未排除 `ev.reporter`）② **warn 通知誤報「進行中」，真正開始時反而靜默**（`emergency-notify.js` 的 `fired` 以 event id 去重，而 warn→start 刻意沿用同一個 id；且 warn 時 `startAt === 0` 使文案走「進行中」分支）③ **插件 payload 的 phase 拼錯或缺值會被靜默當成 start** —— 驗證寫成「不是 end/warn 就當 start」，等於把格式錯誤升級成可信度最高的來源 ④ **插件證實既有手動事件時不校正時間**（`events-do.js` 的 `report()` 只改 `source`，`startAt`／`endAt`／`startExact` 保留手動估值，白白丟掉唯一精確的時間來源）。順帶：重複通報形成的附議也應觸發提前推播。來源: 2026-08-03 後閘 R9

- [x] **B-023** (P2, feature) — ✓ **2026-08-05 定案**：伊弗利特磁暴同一筆事件同時拿到通告變體 `storm-b`（「磁暴造成渴望灣多個地區受災！」）與任務板 `[518,522,530,537,543]`（＝`storm-α` 組）⇒ **`storm-b ↔ storm-α`**。⚠️ **與先前的順序推定完全相反**（原本假設「client 內兩者排列順序相同」⇒ `storm-a→α`），所以那個「※ 對應待實測確認」的記號留對了——網站在定案前對磁暴顯示的通告文字是錯的。定案後 `_fillFromBoard()` 會用 `variant_map` 讓**變體單獨就能判組**，不必再開任務板；一種天氣只需一筆證據，另一組由排除法得出。✅ **2026-08-06 三種天氣全數定案**（線上 `/state.variantMap`）：`storm-b→storm-α`／`spore-a→spore-β`／`meteor-a→meteor-α`，另三組排除法得出。⚠️ 三筆**沒有一筆**符合「client 內排列順序相同」的直覺推定，磁暴甚至完全相反 ⇒ 當初拒絕硬套順序、等共現證據是留對了。

- [ ] **B-030** (P1, perf)【建議 高｜延遲風險 中（下一面牆是 rows-read）｜執行風險 中｜副作用 動線上 DO schema】**DO SQLite 零索引 ＋ `_sweep` 掛在最熱讀路徑**。`events`／`subs` 全表沒有任何 `CREATE INDEX`（實測 `grep -c` = 0），而 `_sweep()` 在每次 `/state` 都跑，約掃全表四趟。2026-08-04 的額度事故是 invocations，**下一面牆是 rows-read**。動工前要決定：① 索引開哪幾欄（`world`+`status`／`endAt`）② `_sweep` 改成多久跑一次（每次讀 → 每 N 秒或掛 alarm）③ 要不要先量一輪實際 rows-read。來源: 健檢 2026-08-08（resilience/R1，confirmed high）

- [ ] **B-031** (P1, fix)【建議 高｜延遲風險 中｜執行風險 中｜副作用 改渲染架構】**每秒無條件重建 DOM ⇒ 焦點被銷毀、點擊有機率被吞**。`emergency-view.render()` 每秒 `replaceChildren` 整份列表（`now-panel` 同型），而被吞的正是**附議／否認／通報**這三個核心動作；鍵盤使用者完全無法操作。修法是差分更新或只更新會變的文字節點，並決定「哪些東西真的需要每秒動」（倒數需要，列表結構不需要）。來源: 健檢 2026-08-08（perf-ux/P1 ＋ a11y/A1，confirmed high）

- [ ] **B-033** (P2, fix)【建議 中｜延遲風險 中｜執行風險 低｜副作用 改通知行為，需對照 2026-08-03 裁示】**「只有預告」事件的三個語意缺口**：① 對只有預告的事件附議**不觸發任何推播**（與 `events-do.js` 自己的註解宣稱相反）② 手動通報撞上預告中的事件會被吞成附議，事件永遠停在 `startAt=0` 並提前約 5 分鐘從畫面消失 ③ 前端網頁通知把只有預告的事件說成「進行中 · 還剩 15 分鐘」（後端 Discord 版有處理，前端漏了）。②③ 是明確的 bug；① 的修法會改變通知行為，需確認是否符合「預告一律不推播」的原意。來源: 健檢 2026-08-08（correctness-core A3/A4/A5）

- [ ] **B-034** (P2, fix)【建議 中｜延遲風險 低｜執行風險 低｜副作用 **需先進遊戲核對**】**開發進度分頁的最後一個施工階段永遠顯示不出來**，第 15 期還會印「還要經過 0 個施工階段」（`dev-stage-view.js:116` 的區間篩選）。⚠️ **不要直接改**：finding 自己的建議是先用遊戲內開拓紀錄核對 `fromStage` 到底是「起」還是「達成」，別在未定性的語意上疊修補（鐵則 §2）。來源: 健檢 2026-08-08（correctness-data/A2）

- [ ] **B-035** (P2, fix)【建議 中｜延遲風險 中｜執行風險 低｜副作用 無】**現況列的操作結果寫進藏起來的元素 ⇒ 失敗完全靜默**。附議／否認／撤回／馬上通知的成功與錯誤都寫進 `el.msg`，而那個元素在**通報彈窗裡**——列上按的人永遠看不到。修法是就地回饋（該列上）或改用 toast。來源: 健檢 2026-08-08（ux-flows/U1）

- [ ] **B-032** (P3, fix)【建議 低｜延遲風險 低｜執行風險 低｜副作用 **行動版尚未 opt-in 屬 Owner 政策**】窄螢幕「臨時任務」面板破版：固定欄寬 grid 溢出約 600px、任務名稱欄被壓成 0 寬。設計系統的行動版政策是**逐工具 opt-in**，本站尚未 opt-in ⇒ 要不要修屬 Owner 決定。來源: 健檢 2026-08-08（a11y/A2 ＋ ux-flows/U2）

- [ ] **B-036** (P3, chore)【建議 低｜延遲風險 低｜執行風險 低｜副作用 需替代 UI】**關鍵說明只掛 `title` 屬性**（`crafter-link.js` 的 `×?`、地圖 pin 的任務名等 7 處）：觸控裝置完全取不到，且違反設計系統「凡說明類提示一律 `[data-help]`、禁原生 title」的鐵則。來源: 健檢 2026-08-08（a11y/A3）

- [ ] **B-037** (P3, chore)【建議 低｜延遲風險 低｜執行風險 低｜副作用 無】`subs` 表**無列數上限也無過期清理**（`events` 有 RETENTION、`subs` 沒有），且 `_fanout` 逐批 `await` 沒有整體 deadline ⇒ 慢或逾時的目標會把後面的真訂閱者往後推。建議加 sweep ＋ deadline，讓延遲變成看得見的計數。來源: 健檢 2026-08-08（sec-backend/A2 partial→low、data-lifecycle）

- [ ] **B-038** (P3, fix)【建議 低｜延遲風險 低｜執行風險 低｜副作用 可能要動 ICE fork】**事件結束時間在插件偵測延遲時偏晚**。ICE log 實測：7 筆有彈窗倒數可對照的事件裡，2 筆偵測延遲 **81／137 秒**，而 `endAt = 偵測時刻 + 1200s` ⇒ 倒數與結束時間整體偏晚 1.4–2.3 分鐘。同時 `startExact` 欄送到前端卻**從未被消費**，插件 start 一律標 `=1`（宣稱精確）。修法二選一：① 插件改送彈窗上的真實剩餘時間（根因，屬 ICE fork，同 B-028）② 站上不再宣稱精確（讓 `startExact` 真的影響顯示）。來源: 健檢 2026-08-08（主迴圈獨立發現，agent 找不到——它們沒有 log）

- [ ] **B-039** (P3, chore)【建議 低｜延遲風險 低｜執行風險 低｜副作用 CF 會開始計費 log】`[observability] enabled = false`（`wrangler.toml`）＋關鍵路徑無 correlation id ⇒ 線上出事後只有分桶計數、重建不了事件序列。共享密鑰路徑（`/admin/*`、插件 `/report`）的失敗嘗試也因此既無限流也無紀錄。來源: 健檢 2026-08-08（observability ＋ sec-backend/A5）

- [ ] **B-028** (P1, fix)【建議 高｜延遲風險 中（每次連續事件都可能再發生一次）｜執行風險 低｜副作用 動 ICE fork，需重 build 插件】**ICE `EmergencyReporter` 沒清掉上一場的 `variant`**。實證（伊弗利特 2026-08-06）：11:07 那場孢子霧結束後，15:09 的預告與 15:14 的 start 都還帶著 `spore-b`，而當下天氣是 `196`（磁暴）；28 秒後插件自己比對到正確的 `storm-b`。**本站已於 2026-08-08 做防禦**（衝突的變體會被丟掉、觀測天氣覆寫推導值），所以壞值不會再寫進網站——但代價是那 28 秒內事件的 α／β 是空的，地圖指不出地點。修法＝新事件開始時把 variant 歸零，比對到才填。⚠️ 屬 `XIVpluginsDev/ICE-Dev`，本 repo 動不到，開在這裡是為了不讓它消失。來源: 2026-08-08 log 對帳

- [ ] **B-029** (P3, chore)【建議 低｜延遲風險 低｜執行風險 低｜副作用 改動歷史紀錄】**線上 id=94 那筆的天氣仍是錯的**（記成孢子霧，實際是磁暴）。2026-08-08 的修正只讓它不再發生，沒有回頭改。要改需要 admin token（`setVariantMap`／`revoke` 那條路），或直接撤銷該筆讓它從歷史消失。⚠️ 撤銷會讓「上次事件結束時間」的間隔統計少一筆——那一欄的用途正是估間隔，值不值得由 Owner 判。來源: 2026-08-08 log 對帳

- [ ] **B-026** (P3, feature)【建議 中｜延遲風險 低｜執行風險 低｜副作用 要替 α／β 取一組使用者看得懂的名字】**事件列顯示是哪一組任務**。B-023 定案後，`ev.group`（`storm-α`）在後端已經填好、也已經送到前端（`/state.events[].group`），地圖也已經直接開那一組——**唯一沒做的是讓人在開地圖之前就知道**。現況 `weatherLabel()` 只寫「磁暴」。卡住的不是資料而是命名：通告編號叫 a／b、任務分組叫 α／β，畫面上同時出現兩套編號會比不顯示更糟。要做就得先決定對使用者叫什麼（例如「磁暴・救災組」直接用任務內容命名，不露 α／β）。來源: 2026-08-06 文案稽核

- [ ] **B-027** (P3, feature)【建議 中｜延遲風險 低｜執行風險 低｜副作用 預告那一列會多一個會動的數字】**預告倒數**。當初不做的理由是「提前量只量到一筆」，該理由已失效，且 2026-08-11 擴充到 **18 筆樣本：17 筆落在 278–281 秒**（4 分 38–41 秒，全距僅 3 秒），1 筆 543 秒。⚠️ 但**不要直接拿中位數當倒數**——那個離群值代表提前量不是常數，倒數歸零而事件沒開始，在這一頁會被讀成「網站說錯了」進而不信任整頁。可行做法是給區間（「約 4–5 分鐘後」）或等樣本再長。來源: 2026-08-06 文案稽核

- [ ] **B-020** (P2, chore)【建議 中｜延遲風險 低｜執行風險 中｜副作用 動 worker 主檔】拆 `worker/src/events-do.js`（**890 行**，2026-08-08 健檢實測；本條建立時是 602 ⇒ 已長 48%）與 `worker/test/http.test.ts`（**1137 行**，建立時 690 ⇒ 已長 65%），**並新增 `modules/emergency-view.js`（743 行，整支是一個 588 行的函式）**—— 兩支都是 2026-08-02 新建的，違反 AGENTS「新檔 >500 行禁止」，當時就該拆。動工前先列「職責 → 檔名」對照表給 Owner 拍板，禁機械切行。來源: 2026-08-03 後閘 R9

- [ ] **B-021** (P2, test)【建議 中｜延遲風險 中｜執行風險 低｜副作用 新增測試相依】前端零自動化測試。後閘的 warn 通知缺陷（B-019 ②）正好是「後端 43 個測試全綠也抓不到」的實例 —— 那是這條最有說服力的理由。至少要蓋：後端離線降級、訂閱伺服器過濾、同事件只響一次、warn→start 通知、四個分頁都開得出來、無水平溢位。把 warn→start 當第一個回歸案例。**B-024 追加兩個現成案例**（2026-08-05）：① `?ev=<id>&vote=confirm` → 落在緊急分頁、該列高亮、確認列文案為附議 ② `?ev=<不存在>` → 顯示「已結束」不白屏。兩者本輪以假後端＋真瀏覽器手動驗過，但**沒有自動化**。來源: 2026-08-03 後閘 R9

- [ ] **B-022** (P3, chore)【建議 低｜延遲風險 低｜執行風險 低｜副作用 無】`worker/src/index.js` 的兩個 token 用字串不等比較，非 constant-time。CF 邊緣前置、token 長度固定 ⇒ 實務可利用性極低，但修起來便宜：抽一支共用的 constant-time 驗證 helper，兩個入口共用並補錯誤 token 的測試。來源: 2026-08-03 後閘 R9
- [x] ~~**B-006** (P3, feature)【建議 低｜延遲風險 低｜執行風險 高｜副作用 需要後端 + 插件回報】緊急任務即時偵測。~~ — ✓ **由 cycle `2026-08-02-emergency-report` 實作**（Owner 2026-08-02 改變裁示）。當時「暫不做」的理由是「不要有人主動偵測」；現在的做法**同時**接受插件自動偵測（ICE `EmergencyReporter`，Owner 本機先跑，用途是累積數據量）與玩家手動通報，兩種來源在 UI 分別標示。原條目：**離線做不到**（Weather#194–197 不在任何 WeatherRate ⇒ 只能伺服器推播），唯一路徑＝Dalamud 插件偵測後 push 到後端，網站顯示。天花板＝覆蓋率等於回報者數量，沒人在線的伺服器/實例就是黑的。架構上是後掛的一層，不影響現有靜態站。**Owner 已表示暫不做**（2026-07-31：「不要有人主動偵測」）。來源: 2026-07-31-cosmic-site

- [ ] **B-007** (P3, feature)【建議 低｜延遲風險 低｜執行風險 低｜副作用 無】深連結收端：`?mission=<id>` 直開某任務、`?job=<id>` 預選職業篩選；出端＝任務需求物接 marketboard `#/item/<itemId>` 查價。未知 id 要 graceful fallback（toast + 清掉自己的參數），禁白屏。來源: `../_NEW-TOOL.md` 深連結 checklist
