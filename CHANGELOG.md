# Changelog

> 日期段落制（cycle 收官為段）；條目含人話「為什麼」，不從 git log 自動生成。格式見 DEVLOOP §4.3。

## 2026-09-02 — 舊網址 `*.pages.dev` 改回 HTTP 301（Google 一個月來把舊網址當本尊）

Owner 搜尋「ff14 成績單」看到的仍是 `pages.dev`、站名顯示「Cloudflare」。GSC 實查：舊 host 今天才被抓、
擷取成功、允許索引，**「Google 所選的標準網址＝受檢測網址」**——`canonical` 指向新網址被否決。
根因＝B-061（2026-08-04）為省 Functions 額度把交接改成 inline JS 跳轉後，舊 host 對爬蟲回 **200 ＋ 整頁內容**；
真人會被 JS 跳走，Googlebot 不會。canonical 是建議、301 才是指令。

- `functions/_middleware.js`：舊 host 的 HTML 導覽請求回 **301** 到新網址同路徑（GET＋Accept text/html＋
  hostname 全等＋無 `?stay` 四條件不變；`Cache-Control: no-store` 保留可回滾）。`?stay` 資料救援門照舊。
- `_routes.json`：可導覽 HTML 路徑列回 include（＝manifest `paths`）。代價＝新網域這些路徑每次開頁一次
  Functions 呼叫；2026-08-28 撞上限的幽靈 `/settings-api` 已修，不是同一個量級。
- 已知代價：還沒來過新站的舊書籤使用者不再自動帶雲端 UUID（到設定面板貼上即可）；inline 交接腳本保留為退路。
- `tests/handoff.test.mjs` ③–⑤ 改斷言 301／Location／no body／CRLF。13 站同步；跨 repo 一致性哨兵全綠。
- 上線後：GSC 對舊 host property 跑「網址變更」，讓 Google 整批搬訊號。
- 續：GSC「網址變更」驗證器抓首頁**不帶 `Accept: text/html`**，回報「找不到重新導向」⇒ 拿掉 Accept 條件、HEAD 也攔。資產早被 `_routes.json` 只列 HTML 路徑擋在 Function 外，該條件已無作用。

## 2026-08-26 — 行動適配：任務清單 10 欄表改逐任務卡

portal 側行動適配改成預設要做（`_DESIGN-SYSTEM.md` §📱 行動適配是預設，不是選配）。

**破口**（393px 實測）：`#mv-table` 的 wrap scrollWidth **410** / clientWidth 361，而 10 欄
按百分比分下去每欄只剩 **19–74px** ⇒「緊急任務」「月藍尾鯰」被擠成**一字一行的直排**，
最後兩欄（宇宙／月球點數）還被切掉。這不是「窄了一點」，是讀不了——截圖實證。

**修法**（≤700px）：表格 block 化、thead 隱藏、每筆任務變一張卡：任務名整寬當標題，
職業圖示／類型 badge／難度一行，開放條件與需求物各自整寬，時限與宇宙／月球點數帶欄名。
- **DOM 只加不減**：`modules/mission-view.js` 逐格寫 `data-label`（新常數 `MV_COL_LABELS`），
  桌面用不到——桌面有 thead。
- 選擇器一律帶組合子（`#mv-table td`），不碰 `.codex-table` 根 selector（design-lint R5）。

**新哨兵** `tests/mission-view-mobile-labels.test.mjs`（基線 8→9）：三條互鎖——
① `MV_COL_LABELS` 長度 == thead 欄數 ② `row()` 真的逐格寫進 `dataset.label`
③ CSS 真的有 `attr(data-label)` 消費端。**拿掉 `data-label` 桌面完全看不出來**，
只有手機會退化成一串無名數字，正是本 repo 家族反覆踩的零回饋訊號形狀。

驗證：393px wrap scrollWidth == clientWidth（361，橫捲消失）、每張卡 143px；
桌面 1366 `tr`/`td`/`thead` 的 display 與 wrap scrollWidth(1288) 全維持原值；
`tools/validate.mjs` 資料不變量全過、`tests/run-all` 9/9。

## 2026-08-12 — 預告改顯示區間；天氣統計擴充到 n=173（並更正卡方基準）

**為什麼**：Owner 問「天氣的 log 應該很齊全了吧」。是——`/history` 累積到 **173 筆**（8/6 那次是 71），涵蓋 9 天、7 台伺服器。資料變硬讓兩件事得以定案，也讓我抓到自己的一個方法錯誤。

### Fixed

- **卡方檢定的基準用錯了**。原本拿觀測值去比**機率表的理論值 70/15/15**——但天氣是每段獨立擲的，9 天只有 550 段，本身就會偏離長期機率（這段期間實際是 65.1/18.5/16.4）。用理論值當基準等於把「這陣子天氣本來就偏多靈風」算到事件頭上：同一份 n=173 用理論值算出 **χ²=10.06（看起來顯著）**，用該期間實際分布算是 **3.35（不顯著）**。
  ⚠️ **2026-08-06 寫下的 χ²=1.48 也是同一個錯算法**——那次結論沒錯只是因為樣本小、剛好沒跨過臨界值。已在 AGENTS 鐵則 §4 更正並寫明理由，否則下一個人重算會以為結論翻案。
- 交叉驗證**排除了回報偏差**：回推 105 個「漏掉」的事件（長間隔＝中位數的整數倍），它們的天氣分布 62.9/20.0/17.1 與觀測到的 59.0/20.2/20.8 一致 ⇒ 不存在「特殊天氣時人比較多所以比較容易被通報」。
- 站上三處引用的數字同步更新（現況面板 help ×2、天氣覆蓋告誡）。其中一處**在公開頁面引用了算錯的基準**，那是必須修不是選配。

### Added

- **預告列改顯示「約 4–5 分鐘後開始」並隨時間收斂**（B-027，Owner 拍板方案 A）。依據＝插件量的 `warnedAt→startAt` 18 筆，其中 **17 筆落在 278–281 秒**（全距 3 秒）。
  ⚠️ **刻意給區間不給精確倒數**：那 1 筆 543 秒的離群值正好證明提前量不是常數，而倒數歸零卻沒開始，在這一頁會被讀成「網站說錯了」而失去整頁信任。走過區間**退回「即將開始」，不改口說「應該已經開始了」**——那 1/18 的場合就是還沒開始，我們不知道就不講。
- `tests/warn-eta.test.mjs`（5 條），含「輸出永遠不得出現秒級文字」的格式斷言——那條是防日後有人把它改回精確倒數。

### Notes

- ❌ **per-server 間隔統計不上站**（Owner 裁示，記為 B-040）。資料夠了（166 個間隔中 124 筆落在 161–326 分、中位 261 分），但放在「上次 03:24 結束」旁邊的任何統計都會被讀成「所以下次大概幾點」，而那個週期是**觀察到的形狀、無 client 欄位佐證** ⇒ 撞鐵則 §4。
- ⚠️ **`/history` 的 100 筆上限是全域的**：事件累積到約 19 筆／日之後，全域查詢只涵蓋 5 天，更早的會滾出視窗（DO 本身留 90 天）。要拿完整序列得**逐伺服器查** `?world=<名>&limit=100`。這條已寫進 AGENTS，否則下次分析會安靜地少一半資料。
- 測試檔 7 → **8**。
- `board-log.jsonl` 仍停在 8/1 的 186 行——條件邏輯／抽選規則（B-013、B-016）再多緊急事件資料也答不了，需要遊戲內 `/ice log on`。

## 2026-08-08 — 全專案健檢：修掉五類「功能靜默失效」

**為什麼**：Owner 要求完整健檢並把不需拍板的項目直接做完。14 維度 fan-out（37 agents、85 findings、70 confirmed／15 partial／**0 refuted**）＋主迴圈加倍抽驗。報告與計畫在 `docs/health-reviews/`。

**雙視角總評：專案體質 6.9 / 使用者友善 6.4**。0 critical、2 high，其餘集中在同一個形狀——**功能在畫面上、實際不會動，而且沒有任何訊號**。

### Fixed

- **路由層丟掉 `weatherObserved`** ⇒ 兩天前才立的鐵則 §5.5「觀測 > 推導」**在正式環境從未生效**。`index.js` 逐欄列舉傳給 DO 的物件時漏了它，於是 `if (input.weatherObserved && ...)` 恆假。當初的測試走 warn→start 升級分支（那條有自己的 UPDATE）所以恆綠——**測試綠而功能死**。已補傳並加「事件已 active 時插件觀測到別種天氣」的測試，反向對照確認移除修正後恰一條轉紅。
- **鬧鐘「開啟當下」（0 分）永遠不會響**。迴圈先跳過所有已開視窗，剩下的 `eta` 必然 > 0，而閘是 `eta > leadSeconds` ⇒ lead=0 一律 continue。選項就掛在畫面上、狀態列還寫著「開啟中 · 提前 0 分鐘」。修法收「剛開的」視窗（60 秒回溯窗——不設下界的話，按下開關的當下會被所有已開視窗一次炸滿）。
- **靈風視窗在連續兩段靈風時提早 23 分 20 秒收掉**。原本用「**目前這個時段**還剩多久」當結束時刻，但天氣是每段獨立擲的——實測 **12.8%** 的靈風後面緊接著又是靈風。新增 `forecaster.currentRunEnd()` 一路掃到邊界。
- **八個錯誤訊息全部退化成「請稍後再試」**。HTTP 層失敗回 `{error}`、Durable Object 的判斷回 `{reason}`，而前端只讀 `error` ⇒ `cooldown`／`not_reporter`／`has_confirms` 這些**早就寫好的訊息從來沒被用過**，全部換成一句「重試看看」——而那些情境重試永遠不會成功。順帶修掉 `bad_lead` 文案的 drift（寫 0–15，實際 −19–5）。
- **CORS 白名單放行任何 `ffxiv-*.pages.dev`**。`*.pages.dev` 是全 CF 共用命名空間，任何人都能開一個 `ffxiv-evil` 取得合法 Origin。同一段註解上面才說「新網域刻意不用萬用比對」，下面卻對 pages.dev 用了。已收窄到專案名（preview hash 前綴保留）。

### Added

- `tests/time-weather.test.mjs`（8 條）——**整站最核心的天氣／ET 純函式在此之前一條數值斷言都沒有**，而「時間資訊要對」正是這個站的核心承諾。釘週期常數、ET 尺度、種子值域、長期分布 vs client 機率表、連續同天氣段邊界。
- `tests/error-messages.test.mjs`——後端錯誤碼 ↔ 中文訊息的涵蓋率哨兵。加上去當場又抓出 9 個沒訊息的碼，其中 3 個（`bad_worlds`／`bad_mention`／`bad_kind`）使用者按得到，已補。
- `tests/alarm-lead.test.mjs`（4 條）——喂假 window 與假 Notification 逐秒跑過開啟時刻，含反向對照。
- **`AGENTS.md` 補 `TEST-BASELINE` 標記**：本 repo 原本沒有 ⇒ monorepo 的 pre-commit gate 6 與跨 repo 稽核**整個跳過本站**，VERIFY 段宣告的測試數從來沒有任何東西在驗。

### Docs

- **README 複述本專案自己已證偽的結論**：「標出緊急任務的必要條件視窗（靈風時段）」——該說法 2026-08-02 就證偽並從站上移除，README 沒跟著改；而且整個緊急事件功能（預設分頁）在 README 裡完全不存在。
- `worker/README` 三處：「不排 alarm」（實際有，靜置期就靠它）、`startsInMinutes` 0–15（實際 −19–5）、缺 `/admin/variant-map`、測試數 38/20（實際 66/41）。
- B-017 關閉——它是 open 條目卻指向**已證偽的修法**（「補齊多個 ToDo 槽」），數字也停在 117。

### Notes

- 測試檔 4 → **7**、worker 整合 64 → **66**、純函式 41。
- ⚠️ **worker 已改但尚未部署**：Worker 與 Pages 部署完全脫鉤，只 push 的話 `weatherObserved` 與 CORS 收窄**線上不會生效**，而且測試全綠、畫面全正常。部署屬對外行為，留給 Owner。
- 未執行的 10 項已開成 B-030～B-039，其中 2 個 high（DO 零索引、每秒重建 DOM）各自需要先寫 spec；B-034 需要**先進遊戲核對**才能動（鐵則 §2）。
- **0 refuted 觸發 skill 的橡皮圖章警語** ⇒ 主迴圈親自複驗 5 條（全部屬實，含自行掃 20000 段實算連續靈風比例）＋5 項 recall 反查。其中 ICE log 對帳抓到 **2 項 agent 找不到的**（它們沒有 log）：事件結束時間偏晚 81–137 秒、`startExact` 送出卻從未被消費 → B-038。

## 2026-08-08 — 緊急事件的天氣會被殘留的變體寫錯

**為什麼**：Owner 問「最近的 log 有沒有助於分析網頁正確性」，比對 ICE 執行 log 時抓到一筆**站上是錯的**紀錄。

**實證（伊弗利特 2026-08-06）**：

```
15:09  送出 warn   variant=spore-b        ← 同日 11:07 那場孢子霧殘留的
15:14  送出 start  weatherId=196（磁暴）  ＋ 同一個殘留 variant
15:14  命中開始通告｜變體=storm-b｜磁暴造成渴望灣多個地區受災！
15:34  送出 end    weatherId=196 ＋ 正確的 storm-b
```

站上 **id=94 記成孢子霧**。地圖會據此指向孢子霧那半張圖。

**兩層缺陷互相掩護**：① 預告建檔時用 `variant` 推天氣——而 `validatePluginReport` 早就刻意把 warn 的 weather 設 null（因為 weatherId 是殘留值），從 variant 推導等於把剛擋掉的錯值從後門放進來，**variant 是同一個殘留問題** ② `warn→start` 升級那一段**從頭到尾沒有寫 `weather`** ⇒ 20 分鐘內三筆帶著正確天氣的回報，一次都沒有機會改正。

### Fixed

- 插件 start/end 的天氣（讀自 `ActiveWeather`）**一律覆寫**先前的值。COALESCE 的「先到的贏」在這裡是錯的——第一筆常常是預告，那時天氣還沒翻轉。
- `warn→start` 升級補寫 `weather`（原本這段只寫 startAt/variant/missionIds）。
- 預告建檔**不再由 variant 推天氣**。
- `weatherKindOf(variant) !== weather` ⇒ **`variant` 與 `groupKey` 一起清空**。清空而不是改成推導值：我們知道那個變體是錯的，不知道正確的是哪一個。留白是本站處理得了的狀態（鐵則 §2）。

### Notes

- ⚠️ **手動通報的天氣沒有覆寫權**：那是使用者自己選的、跟先到的值同一級。第一版改寬了，既有測試當場抓到——「第三個人選錯」會蓋掉前兩個人的正確值。收窄成只有插件 start/end（`weatherObserved`）。
- 既有測試 `預告時沒有變體，開始通告才補上` 的 fixture 本身是**矛盾組合**（`weatherId 196`＝磁暴配 `spore-b`），已改成自洽的一對。它要驗的是「COALESCE 不會用 null 蓋掉」，斷言意圖不變。
- 測試 60→**64** 整合、40→**41** 純函式。新案例逐步覆蓋真實序列；反向對照確認過：不補 `warn→start` 的 `weather` 那一行，第 ② 步就轉紅。
- 🔴 **根因在插件，不在本站**：ICE 的 `EmergencyReporter` 沒有在新事件開始時清掉上一場的 `variant`。本站這輪做的是**防禦**（壞值進不來），插件那邊要另外修。
- ⚠️ **線上 id=94 那筆仍是錯的**——本輪只讓它不再發生，沒有回頭改歷史（需要 admin token）。

## 2026-08-06 — 有配方的任務全部補上求解（含緊急任務）

**為什麼**：Owner 要求「任務清單有配方的全部補上求解，包含緊急任務」。現況是 **33 個緊急任務一個求解連結都沒有**——它們有配方（24 筆）卻抓不到需求物，而連結是掛在需求物上的。全站「有配方卻連不了求解」共 **40 筆**（24 緊急＋16 雙職業）。

**AGENTS 記了一年的假設是錯的**：舊註解說「`WKSMissionUnit` 有多個 ToDo 槽，本站只讀 col[11] 一個」。逐欄檢定後——除 col[11] 外唯一像 ToDo FK 的是 col[12]（恰好 33 筆＝全部緊急任務、值＝col[11]+1），**而那些列同樣沒有需求物**。多讀幾個槽解決不了任何事。

**真正的路是配方**：360 個「ToDo 有需求物、且有配方」的任務裡，需求物 **360/360** 都落在該任務配方的產出集合內（`Recipe.c4`，零例外）；而那 40 筆**每一筆都剛好只有一個配方** ⇒ 沒有「要交哪一個」的歧義。語意也對得上：「製作抗震貨箱」→ 宇宙探索用的貨箱、「製作新型菌絲建材」→ 超輕菌類建材。

### Fixed

- 求解連結 **360 → 400 筆**（＝所有有配方的任務）。緊急任務從 0 個變成 24 個可求解，另 9 個是採集／釣魚類本來就沒有配方。
- 40 個新連結**全部在 crafter 的配方表查得到**（實測 0 落空）。

### Notes

- ⚠️ **數量推不出來，所以不填**：`WKSMissionRecipe` 只有配方 id、沒有任何數量欄，而已知需求數量分布是 ×1/×2/×3/×5…×48，沒有常數可套。這些需求物的 `qty` **缺席**（不是 0、不是 1），UI 顯示橘色的 `×?` 並在 hover 說明原因。省略會被讀成「一個」、補 ×1 是直接給錯的備料量——兩種錯法都不會有任何訊號（鐵則 §2）。
- `validate.mjs` 兩條新斷言釘死這件事：由配方補的需求物＝40 筆、且**不得帶 `qty`**。原本「117 個抓不到需求物」的追蹤數字同步降到 77（剩下的是討伐／偵察類，真的沒有要交的東西）。
- `Recipe.c4` 的欄位索引放在本 repo 的 `TcCosmicSheets.cs`（`Recipe` 不屬於 ICE fork 管的那三張表），反解依據寫在該處註解：520/520 產出都是名稱非空的合法 Item，其餘候選欄不是全零就是明顯不對（c1 全是 Gil、c3 出現「過期牛皮牧羊腰帶（漆黑）」）。

## 2026-08-06 — 頁首不再擠成一片

**為什麼**：Owner 回報「最上方有點擁擠」並附截圖。量下來不是感覺——**BETA 告示、四格、分頁列三者的間距literally 是 0**（`betaBottom === statsTop === 169`、`statsBottom === tabsTop === 287`），三塊直接黏在一起。同一張截圖還露出第二個問題：靈風那格的值被拆成「6 小時 12 分後（本地」／「18:30）」。

### Fixed

- `.cos-stats` 補上下 margin（16 / 24px）。這條之前沒有，是因為四格原本住在分頁內、被面板的內距撐開；2026-08-03 挪到分頁列之上變成全頁共用之後就直接貼住了，而沒有任何東西會提示這件事。
- **「（本地 18:30）」不可拆**（`.cos-stat__clock` nowrap）。斷在「後」與「（」之間是乾淨的，斷在括號裡不是。
- **「目前天氣」的註記改成三個語意段各自 nowrap**，斷點只可能落在「→」與「·」上。原本會切在詞中間——Owner 截圖上是「接著是 🌤 晴」／「朗 · 月塵…」。
- 四格最小欄寬 200 → 240px：太窄時每一格都會自己內部亂折，寧可讓四欄折成兩排。
- BETA 告示補內距（12/16px）：共用 tint-panel 的預設 padding 是給列與小方塊用的（8/12px），這是一條橫跨整頁的散文告示。元件本身明文允許消費端覆寫 padding。

### Notes

- 四格的值高度現在四格一致（31px，全單行），卡片等高 135px。四個分頁與 1420／1920 兩種寬度都驗過 `scrollWidth - clientWidth === 0`、console 零 error。

## 2026-08-06 — ET 正名：艾歐澤亞 → 艾奧傑亞

**為什麼**：Owner 指正「艾奧傑亞才是 TC 翻譯」。查證屬實——台服 client dump（`data/item_dict/datamining_tc/`）的 `tc_Action`／`tc_Fate`／`tc_Item` 都出現「艾奧傑亞」，**「艾歐澤亞」零筆**。後者是國際服／簡中式音譯，違反全域鐵則「繁中服至上、禁日服音譯／自創譯名」。

### Changed

- 全 repo 25 處改用台服官方譯名（11 個檔，含註解——註解裡留著錯譯，下一個人照抄就又回來了）。
- **`data/missions.json` 走重跑產生器，不手改**（鐵則 §1）：ET 條件的 label 是 `Exporters.cs` 組出來的，只改產生器不重跑 dump 的話**站上顯示的仍是舊譯名而原始碼看起來已經對了**。實跑 `dotnet run -c Release --project tools/cosmic-dump`，健全性閘全過（544 任務／天氣總和 100%／11 條工具鏈），diff 僅 `generatedAt` ＋ 12 條 label，client 版本不變（2026.07.22）。

### Added

- 譯名哨兵（併入 `tests/clock-labelling.test.mjs`）：掃 `modules/`＋`index.html`＋`tools/cosmic-dump/*.cs`＋**`data/*.json`**。最後一項是關鍵——它抓的正是「源碼改完、產物沒重跑」這個狀態，而那個狀態**在畫面上是錯的、在程式碼裡卻是對的**。
- ⚠️ 哨兵**刻意不掃 `AGENTS.md` 與測試檔本身**：規則必須寫得出「不要用哪個詞」，掃它們等於規則一寫下去就自我違規（第一版真的紅了）。凡是「規範某個字串」的哨兵都有這個坑。

## 2026-08-06 — 版面收斂到共用元件（Owner：「主用共用元件」）

**為什麼**：Owner 要求「頁面美化跟整齊」＋「主用共用元件」。盤點下來真正的亂源不是版型，是**同一個東西在本地被刻了三份**。

### Changed

- **三份逐字相同的「警示色左邊條」自刻收掉**（`.cos-betanote`／`.cos-np__caveat`／`.cos-caveat`，全都是 `border-left: 3px warn ＋ radius ＋ surface 底`）→ 共用 `.codex-tint-panel--warn --bar`。設計系統的 B-017 Track B 就是為了收編這一派，本站當初沒接上。本地只留 `margin`。
- **四格的框與底色改吃共用 `.codex-tint-panel`**。這裡原本是**真的在打架**：本地 `.cos-stat` 宣告 `border: 1px`，而靈風那格疊的 `--bar` 宣告 `border: 0`——兩者同為單一 class，靠檔案載入順序決定勝負，結果金色那格同時有全框和左邊條。
- `.cos-np__caveat` **不再降 muted**：它是「使用者需要讀懂才能操作」的說明文字，屬設計系統 muted 準則的禁用範圍。
- **四格註記文案收斂**，三格從折行變單行：① 目前天氣那句原本會產生**巢狀括號**「…（月塵 26 分後（本地 11:53））」，折行時行首還留一個孤立的「（」→ 改用「·」串接 ② ET 那格拿掉與 label 重複的「遊戲內時間」 ③ 靈風那格「（佔 15% 時段）」原本被斷成孤字「段）」。

### Removed

- 死碼 `.cos-highlight` / `.cos-highlight__title` / `.cos-np__progress`（HTML 與 JS 皆零引用，逐一 grep 反查過）。
- ⚠️ 同一輪掃出的 `.cos-map__*--g0/--g1` 是**誤報**，`emergency-map.js` 用 `` `cos-map__dot--g${groupIndex}` `` 動態組出來——靜態掃描看不到，照刪會讓地圖的分組配色整組消失。

### Notes

- 沒有動 `.cos-np__btn` 那組手刻的 grid 列。它看起來像「該換成 `codex-table` 的假表格」，但**實測兩張清單的欄位起點逐欄相同**（557/617/977/1274…），標題與資料只差 1px（標題無邊框）——它沒有壞，換掉是重構不是整理。
- 四個分頁與四種寬度都驗過 `scrollWidth - clientWidth === 0`。

## 2026-08-06 — 本地時間與 ET 一眼分得出來

**為什麼**：Owner 指出「靈風視窗那些有本地時間 XX:XX 的，要讓玩家清楚知道這是本地時間跟 ET 的差別」。頁首四格並排著 `艾奧傑亞時間 15:42` 與 `靈風視窗 7 小時 24 分後（18:30）`——**兩個時鐘格式一模一樣**，而人是掃數字的，label 幫不上忙：`18:30` 很容易被讀成 ET 的 18:30。

### Changed

- 散文裡的現實時鐘一律輸出 **`本地 18:30`**（新 helper `localClockText()`），ET 那一格的值改成 **`ET 15:42`**（`etClockText()`）。四格並排時就是一組對照，不必先讀 label。
- ET 那格的註記從「天氣全伺服器同步，不分伺服器」改成 **「遊戲內時間 · 1 小時＝現實 2 分 55 秒；全伺服器同步」**——「差別」的本體是**尺度**（ET 比現實快約 20.6 倍），不講尺度只講名字等於沒講。
- 「還剩 12 分（**到** 本地 11:06）」讀起來卡 ⇒ 改寫成「還剩 12 分（**本地 11:06 結束**）」，保留「這個時刻是結束不是開始」的語意。
- 緊急事件歷史表新增表頭副標「本地時間」（該欄只有 10% 寬，塞不下行內標記）。
- 四格的值加標記後會**折行**，所以時鐘降一級字（`codex-small`）。折行只是觸發點——這一格的焦點是倒數，時鐘是拿來對錶的輔助，本來就不該同級。依設計系統「utility 只裁字級」，顏色字重仍繼承。

### Added

- `tests/clock-labelling.test.mjs` — 漏標是**零回饋訊號**缺陷（畫面正常、無錯誤、全綠，只有人看錯時間跑去等），所以要哨兵。斷言：裸 `clockText(` 只准出現在白名單檔案且**次數精確吻合**（只寫「允許這個檔」的話，同檔再漏一處仍會綠）／兩個 helper 的輸出格式／反向控制「兩者不得輸出成同一種東西」。測試檔 3→4。

## 2026-08-06 — 「特殊天氣會觸發緊急事件」正式證偽，站上錯誤說明移除

**為什麼**：Owner 問「目前資料夠不夠判出特殊天氣與緊急事件的關係——是靈風才會轉，還是晴朗也會直接轉」。夠了。8/2 起累積的 **71 筆通報**（`/history`）每一筆都能回推當下的演算法天氣（天氣是時間的純函數），結果是**晴朗 45／月塵 13／靈風 13**，對照基準機率 70/15/15，χ²=1.48（df=2，臨界 5.99）⇒ **與隨機取樣無異，45 筆是從晴朗直接轉過去的**。

兩條佐證：事件開始距天氣時段起點**均勻散布**（17–1395 秒／週期 1400）⇒ 跟天氣時鐘完全不對齊；真正的形狀是 **per-server ~4 小時 14 分的計時器**（同伺服器間隔 64 筆中 47 筆落在 214–311 分，其餘全是近整數倍＝那幾輪沒人通報）。

⚠️ **這不代表觸發規則解出來了**。已排除的只有天氣；週期是觀察到的形狀、無 client 欄位佐證 ⇒ 鐵則 §4「不預測、不排程、不猜下一次」原封不動。

### Fixed

- `now-panel.js` 的告誡列：舊文案「⚠️ 現在是月塵，**隨時可能觸發**緊急事件」暗示天氣會觸發事件（錯），「本站也**偵測不到**它正在發生」在 8/2 有了緊急事件分頁之後也已經是錯的。改成講**真正還成立的那件事**——緊急事件一發生就覆蓋當前天候、上面的天氣限定任務會失效——並指向緊急事件分頁。
- **告誡列仍然只在靈風／月塵出現**，但理由換了：不是「那時比較容易出事」，而是**天氣限定任務只綁這兩個天氣**（20＋21 個，晴朗 0 個）⇒ 晴朗時這段話沒有指涉對象。
- `index.html` 現況面板說明：補上「與現在的天氣無關」的實測數字；「提前量只量到一筆（約 5 分半）」已過期，改為 11 筆的實況（10 筆 4 分 39 秒、1 筆 9 分）。
- `now-panel.js` 排除緊急任務的理由註解：舊註寫「天候條件只是必要條件」，但 2026-08-02 定性後 **33 個 `critical` 的 `conds` 實測 0/33 全空**——緊急任務跟天候條件毫無關係，當初會列出來純粹是 c14/c15 判錯欄位。

### Notes

- **B-023（通告↔任務分組）三種天氣全部定案**：線上 `variantMap` ＝ `storm-b→storm-α`／`spore-a→spore-β`／`meteor-a→meteor-α`，另三組排除法得出。⚠️ **沒有一筆符合「client 內排列順序相同」的直覺推定**，磁暴甚至完全相反 ⇒ 當初拒絕硬套順序、堅持等共現證據，是留對了的一次。`variant-groups.js`／`emergency-map.js`／`emergency-view.js` 裡「目前無證據」的註解一併更新。
- 資料鏈已通到底：後端 `_fillFromBoard()` 用 `variant_map` 讓**通告單獨就能判組** → `events[].group` → 地圖直接開那一組。**缺的只有「開地圖之前就看得到是哪一組」**（B-026，卡在 a／b 與 α／β 兩套編號的命名，不是資料）。
- `events-do.js` 的 `variantMap` 註解原本寫「前端據此顯示※待實測確認」——前端從來沒消費過這一欄，已改為實話。
- 分析可重跑：`/history?limit=100` ＋ `modules/eorzea-time.js` 的 `forecastTarget()`，不需要遊戲 client。

## 2026-08-05 — 展平 ES module 載入瀑布（B-025）

接在同日「各伺服器現況」那則後面：那則拆掉的是**串行鏈的最後一段**（現況不必等四份 JSON），這則拆的是**中間那段**——「app.js → 20 支 module」本身也是逐層的。

**病灶**：module 的依賴只有在父模組下載並解析完之後才會被發現，孫層又要等子層。同型病灶在 market 站量到 production 排成 5 波（0→300→360→420→540ms），同一批檔平行抓只要 150ms。

**修法**：`index.html` 加 19 條 `<link rel="modulepreload">`（`modules/app.js` 的靜態 import 圖），瀏覽器解析 HTML 時就一次全部平行抓。純新增標籤，不動邏輯、不改模組求值順序。

⚠️ **清單生成不手貼**：19 條 `<link>` 是依賴清單的第二份複本，漂掉的症狀是**開頁慢一點**——無錯誤、無警告、build 全綠。由 `tools/gen-modulepreload.mjs` 從 import 圖推導，哨兵機械比對。

⚠️ **生成器與哨兵是 13 站樣板，各站只准差一行**（`export const ENTRY`，本站是 `modules/app.js`、market 是 `app.js`）。入口名由生成器 export 給測試用，不在測試裡再寫一次——寫兩次等於副本間差兩處，抄的時候會漏。哨兵檔本身與 market 版**逐字節相同**。

### Added

- `tools/gen-modulepreload.mjs`／`tests/modulepreload-drift.test.mjs` — 自 market（B-081）複製。四條反向斷言：集合雙向相等／入口不得自我預載／href 對應檔案須存在／區塊須與生成器輸出逐字節相同。

## 2026-08-05 — 「各伺服器現況」不再排在四份離線 JSON 後面

**為什麼**：Owner 回報「緊急事件的各伺服器現況要等很久才展開」。實測後**後端不是慢的那個**——`/state` 只花 25–80 ms，但要到頁面載入 **400 ms 之後**才輪得到它發請求：它寫在 `await Promise.all([四份靜態 JSON])` 之後，於是被排到「HTML → app.js → 20 支 module → 4 份 JSON（含 470 KB missions）」這條**四層串行鏈**的最尾端，冷快取／手機網路下每一層都是一個 RTT。而現況跟那四份離線資料**沒有任何依賴關係**。

### Fixed

- `/state` 改在 app.js **進入靜態資料 `await` 之前**就發出（`prefetch`），view 建好後直接接手那一發。背景分頁不發（鐵則 §5，切回前景時 `visibilitychange` 會補）。
- `poll()` **拿到就重畫**。原本非 force 的輪詢只更新資料不重畫，七列要等到下一個 1 秒 tick 才出現——首次載入最明顯：畫面停在「載入中…」，而資料其實早就到了。`force` 參數隨之消失（每次都畫，沒有第二種行為）。

### Notes

- 預抓帶時間戳，**超過 10 秒就丟掉重打**：載入後馬上切走、幾分鐘後才回來的分頁，那一發已是舊的，而 `fetchedAt` 蓋的是取用當下的時間 ⇒ 會把舊現況標成「剛更新」。在這一頁那不是顯示瑕疵，是把「我們不知道」講成「查過了沒有」（鐵則 §4）。
- 新增 `tests/emergency-view.test.mjs`（手寫 DOM stub，不引 jsdom——要驗的是呼叫時序不是排版）：釘住「首次吃預抓、零額外請求、同一輪畫出七列」「過期預抓改打新的」「背景分頁零請求」三條。已做反向對照：回退到修正前的 `modules/` 該測試轉紅。
- ⚠️ **前景時序無法在自動化瀏覽器裡量**：擴充功能開的分頁本身就是 `document.hidden`，量到的「都沒打」只是撞到 hidden 閘（AGENTS §5 已載明的坑，這次又踩一次）。上面的 400 ms 是在**正式站**用 `performance.getEntriesByType('resource')` 量到的真實資源時間軸。

## 2026-08-05 — Discord 通知可以直接點進來附議（B-024）

**為什麼**：通知原本只有一行字、沒有任何連結。想確認或想附議的人得自己找書籤回站上、切分頁、找到那一列——而附議正是這功能的命脈（可信度靠附議、下架靠否認）。等於把「人正在看 Discord、正在遊戲裡」這個最好的出手時機擋在流程外。

### Added

- 通知改成 **Components V2 Container**：色條包住整塊、標題可點，框**內**三顆按鈕「看事件／✅ 我也看到了／🚫 查無此事」。**預告那一則也有**——預兆通告本身就是遊戲內看得到的東西，「我也看到了」在預告階段有明確指涉；而預告最早、最缺旁證，那時收到的附議會沿用到事件開始那一則（同一個 event id）。
- 點按鈕回站上會落在那一列（高亮）並跳出確認列，**再按一次才送出**。刻意不自動投票：Discord 會替連結預抓、手機上按鈕很近，而否認一旦達標是單向下架、不因後來的附議回頭。
- Discord 若拒收 Components V2（400）→ 自動退回純 embed 版重送一次，**不計失敗、不熔斷**。

### Notes

- 送出網址**必須帶 `?with_components=true`**：不帶的話整組按鈕被 Discord **靜默忽略**——訊息照樣 204、按鈕全部消失、零錯誤訊號。
- 三件實測過**做不到**的事，別再試：彩色按鈕（`style:4`+`url` → 400 `{"components":["0"]}`；彩色 style 一律要 `custom_id`＝互動元件，非 application-owned webhook 禁送）／按鈕文字上色（`label` 是純文字欄位，不吃 markdown）／拿掉按鈕右邊的外連圖示 ⧉（Button 物件 9 個欄位沒有一個控制它，由客戶端固定渲染）。
- Components V2 對非 application-owned webhook **沒有官方明文保證**，我們是靠實測知道可行的——退回路徑就是為這件事準備的：**版面退化，通知不中斷**。
- **部署與驗收（2026-08-05）**：Pages＋worker 皆已上線（worker version `ddec1859`）；深連結收端在正式站以真事件 `id=73` 實測（確認列／高亮／URL 清除／取消不投票）。⚠️ **線上 DO 的 fan-out 路徑沒有端到端實測**——驗它得在站上真通報一筆，那會讓所有訂閱者收到假警報 ⇒ 不自行觸發；該路徑由整合測試守（signal 隔離、`fanout_v2_reject` 分桶、含 query 的 webhook）。

## 2026-08-03 — 舊網址交接頁（monorepo B-048 Task 4，第 9 站）

**為什麼**：本站已掛上 `cosmic.xivtc.com`，但手上是舊 `*.pages.dev` 書籤的使用者不會知道，也不會把跨工具身份（UUID）帶過去。

**為什麼不是 301**：301 在**邊緣執行、早於任何 JS** ⇒ 舊 origin 完全沒機會讀 `localStorage` 裡的 UUID。純 301 會讓使用者靜默失去雲端身份。所以必須回一頁極簡 HTML、由 client 讀 LS 後自行組目標 URL——這也是為什麼目標 URL 不能在 server 端組完就送：**`#fragment` 永遠不會送到伺服器**。

### Added

- `functions/_middleware.js`（**四個條件同時成立才攔**：`GET`／`Accept` 含 `text/html`／host 精確等於 production 舊 host——字串全等，順帶讓 CF preview 子網域天然放行）
- `_routes.json`（**完整枚舉**，刻意不用 `/*`：那會讓每個 CSS/JS/圖片請求都變成一次 Functions invocation）
- `tests/route-manifest.json`（攔截路徑唯一事實源）＋ `tests/handoff.test.mjs`（四個攔截條件各有正負案例）
- `deploy-allow.txt` 加 `functions`／`_routes.json`；`deploy-deny.txt` 加 `tests`（fail-closed，漏了 build 直接失敗）

### Notes

- 本站為**單頁站**，manifest＝`["/", "/index.html"]`。`/index.html` 必須顯式列入——實測它**不會** 308 到 `/`（其餘 `.html` 深鏈會，被 308 後由無副檔名形式接手，交接照樣發生）。
- middleware 由樣板產生，**除兩個常數外與其餘 12 站逐字節相同**，由 monorepo 交接頁一致性哨兵把關（斷言的是逐字節相同，不是「條件有沒有在」——後者抓不到語意被改寫）。

## 2026-08-03（三）— 通報靜置期：誤按不再驚動所有人

### Added

- **辨識「這次是哪一組」改用任務板，不再靠通告文字**（2026-08-03 利維坦實測推翻原設計）。
  原本整套 A／B 建立在「聊天欄會播 client 解出的那六則開始通告之一」上。實測：磁暴事件開始時
  聊天欄**只有一句「發生了緊急情況。」**，那六句一句都沒出現（Owner 推測是任務 NPC 附近的
  區域訊息）。前提不成立 ⇒ 那六句已從畫面移除，不再宣稱它們是聊天通告。
  - 改用**任務板**：任務 id 直接決定組別（`resolveGroup()`），存進事件的 `groupKey`，
    `/state` 帶出 `group`。地圖從那一列點進去就直接開對的那一組並標「✓」，沒有推定、沒有記號。
  - **補送**（Owner：「不可能一直開任務板」）：事件開始那一秒板子多半沒開，插件讀不到
    `missionIds`。玩家之後打開任務板時插件再送一次，後端**只補空欄**把組別補上——
    把偵測綁在天氣翻轉那一秒本來就會漏。
  - 實測（本機）：先送空 `missionIds` → `group` 為 null；再補送 `[537,543,518]`
    （＝Owner 那次真實事件的任務）→ 同一筆事件補成 `storm-α`，畫面顯示「✓ 這次的事件就是這一組」。
  - 順手修一個**靜默**的 bug：改成「一種天氣一行」之後 `render()` 還在遍歷 `picker.children`，
    而那層已經是 row 容器不是按鈕 ⇒ 選中狀態永遠不亮，**且不報錯**。

- **歷史紀錄加「顯示筆數」上限與「距上次」欄**（Owner 2026-08-04）。
  - 上限：最近 20／50／100 筆（預設 20），註記寫明「另有 N 筆未顯示」。切換不重抓後端。
  - 距上次：同一台伺服器距前一次事件多久。**跨伺服器不算**——七台各自獨立發事件，
    混在一起算出來的「間隔」不對應任何真實的東西。
  - **間隔只算「已結束」的**（Owner 2026-08-04）：撤銷／撤回／存疑的**不是事件**，
    拿它們當端點算出來的間隔不對應任何真實發生過的事。判準與後端「上次結束時間」一致。
    實測：同一台 23:45 → 00:45（已撤銷）→ 01:45，最新那筆算出 **2 小時**（跳過撤銷那筆），
    不是 1 小時。
  - **「隱藏已取消」預設開**，且只影響顯示不影響數字（間隔本來就只拿已結束的當端點）。
  - 三個篩選排成一列：原本垂直堆疊在一張很寬的表上方佔三行、右邊全空（150px → 63px）。
  - ⚠️ **先算間隔、再截斷**：反過來的話被截掉的那一筆會少掉「前一次」而顯示 `—`，
    看起來像「沒有前一次事件」，但真相只是它被上限藏起來了。後端一律抓 100 筆當算料。

- **地圖不再講資料是怎麼來的**（Owner 2026-08-05：「不要顯示什麼插件從哪裡讀到的，
  就說是這張地圖就好」）。`✓ 這次的事件就是這一組（插件從任務板讀到的）` → `✓ 這次的事件就是這一組`；
  未確認時改講「這一組的任務：…」。與 2026-08-02「不顯示回報來源」同一條原則。

- **B-023 定案：`storm-b ↔ storm-α`**（2026-08-05 伊弗利特磁暴，同一筆事件同時拿到通告變體
  與任務板任務 id）。⚠️ **與先前的順序推定完全相反**——原本假設「client 內兩者排列順序相同」
  推出 `storm-a→α`，實測是 `storm-b→α`。網站在定案前對磁暴顯示的通告文字是錯的，
  而那個「※ 對應待實測確認」的記號正是為此保留的。
  定案後 `_fillFromBoard()` 會查 `variant_map`，**光靠通告變體就判得出組**（不必開任務板）；
  一種天氣只需一筆證據，另一組由排除法得出。流星雨／孢子霧尚未定案。

- **預告階段就能判組**（Owner 2026-08-04：「有玩家可以提前知道採集地點位置」）。
  補送原本只在天氣翻轉之後才跑，等於把預告那 5 分鐘整段丟掉。現在預告階段插件也讀任務板，
  後端在 `warn` 分支同樣補空欄 ⇒ **事件還沒開始就知道是哪一組**（`startAt` 仍為 0，不假裝已開始）。

- **任務板沒有緊急任務時不再退件**（2026-08-04 實測踩到，正在造成資料遺失）。
  插件送 `missionIds` 時，後端原本檢查「有給就至少要有一個 critical，否則 400」。
  但**任務板顯示什麼取決於玩家當下的職業**，而緊急事件只涉及部分職業——12:18 伊弗利特
  流星雨，玩家是裁縫師（`meteor-α`／`β` 都沒有裁縫師）⇒ 板上只有一般任務 `[175,248]`
  ⇒ **整筆 start 被退掉，事件開始完全沒被記錄**。
  這正是那段程式自己註解裡警告過的事：用一個擋不住偽造者的檢查，換掉真實訊號。
  改為只濾掉非緊急區間的 id、不退件；插件端也一併濾（兩邊都不該把雜訊當證據）。

- **預告一律不推播**（Owner 2026-08-03：「預告也不要先通知，大約 1~3 分鐘內確定了才通知」）。
  一晚出現三筆假預告、全部**立刻**送到訂閱者手上，而通知送出去就收不回來（撤銷只影響網站顯示）。
  預告是所有訊號裡最不可靠的一種，誤判成本卻最高。事件本身立刻就在網站上看得到，**延後的只有
  推播**；真正該觸發通知的是確定發生——天氣真的翻轉（`start`）或有人附議。
  ⚠️ 原本 54 個測試裡**沒有任何一個釘住「預告會不會推播」**，所以它可以靜默存在——已補上。

- **預告階段不再帶出天氣**（同日自己引入、當天就被實測抓到）。插件在預告時照樣會送
  `weatherId`，送的是 `lastEmergencyWeather` 的**殘留值**（初始值 196＝磁暴）；而上一條改成
  「由 weatherId 查表填天氣」之後，那個殘留就被當成觀測結果寫進事件——利維坦的預告因此
  被標成「磁暴」，完全沒有觀測支撐。**兩邊都修**：後端 `warn` 一律回 null，插件 `warn` 送 0。

- **插件回報的天氣種類改由 `weatherId` 查表得出**（2026-08-03 利維坦 #34 實測）。原本天氣只從
  `variant` 前綴推導，而 `variant` 常常是 null（沒抓到開始通告那一刻）⇒ **插件覆蓋到的那一台
  反而顯示「天氣未填」**，可信度最高的來源給出最少的資訊，正好反了。
  對照表出自 client 的 `Weather`（194/195 Meteor Showers、196 Astromagnetic Storms、
  197 Sporing Mist），不是推測。⚠️ **194 與 195 都是流星雨**——id 四個、天氣三種，不是一對一。

- **「開始通告 ↔ 任務組」的定案機制**（B-023 的骨架；Owner 2026-08-03「1、2 都做」）。
  地圖上的 A／B 一直是**順序推定**，這一輪把它變成可以被證據取代的東西：
  - **自動**：插件回報時 `variant`（通告解出）與 `missionIds`（任務板看到的）**同時**存在的那一筆
    就是證據。原本 `missionIds` 只驗不存 ⇒ 每次事件都把答案丟掉；現在存下來並即時判組。
  - **判不出來就留白**：命中 < 2 筆（可能是上一次事件的殘留）或同天氣兩組都有命中（資料矛盾）
    一律回 null，**不回「比較像」的那一組**——這個結果會被寫成永久對應。
  - **一個變體只學一次**：同一個變體出現兩種答案代表證據互相矛盾，那要人去看，不該自動選一個。
  - **人工路徑**：`POST /admin/variant-map`（Owner 在遊戲裡看到就能直接寫，不必等插件剛好
    覆蓋到出事件的那一台）。
  - **一種天氣只需一筆證據**：兩則通告對兩組任務是雙射 ⇒ 定案一邊，另一邊由排除法得出。
    這是推論不是猜測，所以另一邊同樣不掛待實測記號。
  - **分組表抽成 `modules/variant-groups.js` 前後端共用**——後端另存一份就是第二份真相，
    而這份表的正確性是靠台服 client 幾何驗出來的，漂移之後沒有任何機制會發現。
  - 實測（本機）：故意把對應設成**與順序推定相反**的值，畫面上的通告立刻翻過來且記號消失，
    另外兩種天氣維持推定 ⇒ 畫面確實吃證據，不是把猜測寫死。

- **緊急任務地點圖**（Owner：「有辦法渲染地圖然後標記出來目前緊急給哪個職業、要去哪個地點嗎」）。
  座標與底圖**全部來自台服 client**，沒有一個數字是人工標的：
  - 任務地點＝`WKSMissionUnit → WKSMissionToDo.c18 → WKSMissionMapMarker`（欄位索引沿用 ICE fork
    那份唯一定義，不在本 repo 抄第二份）。33/33 緊急任務都有座標，寫死進 `validate.mjs`。
  - 底圖自己從 `ui/map/c1w1/21` 匯出（512²）。**不能 hotlink xivapi**——本站 `_headers` 的
    `img-src 'self' data: <portal>` 會靜默擋掉，生態內其他工具那套在這裡走不通。
  - `Map` 表的 territory 連結是 **c16**（全表只有這一欄出現過 1237）。中途採信過
    `TerritoryType.c26`，它指到的是**異聞六根山**，交叉驗證 1/522——值合理、查得到、
    完全沒有訊號，正是鐵則 §2 那種錯。
  - 換算公式與 monorepo 其他工具同一條，且用兩個獨立來源對過：任務 518 算出 (24.8, 32.6)
    對 ICE 記載的 (24.7, 32.5)；512 算出 (20.0, 37.0) 對 (19.9, 36.9)。
  - **順帶佐證了上游的變體分組**：33 筆完整分割無遺漏，每組製作任務與該組錨點差 4–19 單位；
    差得遠的 8 筆全是採集／釣魚職，因為上游存交付點、client 存採集區 ⇒ client 的更細。
    Owner 那次磁暴實測（採掘＋漁師一處、甲冑＋金工＋煉金一處）只有 α 是這個形狀。
  - **按鈕不掛 A／B**（Owner：「會有點誤導」）：通告分得出兩則、任務分得出兩組，但
    兩者之間**沒有證據**（B-023）。標籤改用「這組有什麼任務」，通告原文照列並註明對應未定。
    同理不做「跟著現況自動切變體」——那只會有一半機率切對，而使用者不會知道它切錯了。
  - 預設收合、小圖、職業圖示直接畫在座標點上（Owner 三次調整）。
  - **範圍圈用 client 的半徑欄畫**（`WKSMissionMapMarker` 的 c3：採集區 80／100／120，
    交付點 0）——就是遊戲地圖上那個圈。**0 不是缺值，是「這裡是一個點」**，兩者長得不一樣。
    Owner 原本想用遊戲的 FATE 圖示，但那組（icon 60501–60508）是**依 FATE 類型**分的
    （討伐／收集／護送），掛到我們的任務上等於標一個不相干的類型。
  - **地點與標籤分開畫**：兩個地點只差幾個百分點時標籤會疊成一團（Owner 截圖），
    但把點也移走就等於畫錯位置 ⇒ 圈／點釘在真實座標，只有標籤上下讓開。
  - **一筆事件一張圖，不共用**（Owner：「不要共用地圖，到時候每個緊急一起出你怎麼顯示」）。
    最初做成頁面底部一張共用的圖，**兩台同時出不同天氣時那張圖必然有一台是錯的**。
    改成彈窗，由現況那一列打開，伺服器與天氣都由那一列給；天氣已知就只列那個天氣的兩組。
  - **入口＝現況那一列的天氣標籤**，而且**天氣沒填也要有**——第一版把它掛在天氣標籤上，
    結果沒填天氣的事件整列連一個入口都沒有（Owner 截圖：奧汀「天氣未填」，畫面上找不到地圖）。
    而「不知道天氣」正是最需要看地圖的情況。同時那個標籤**不再寫 A／B**——地圖上沒有叫
    A／B 的組，寫了會讓人找不到對應的那一半。
  - 內容漏了 `.codex-accordion__body`（設計系統的內距契約層），整塊貼著卡片邊緣（Owner 截圖）。
  - **底下另有「任務地點速查」**（Owner 2026-08-03）：不綁伺服器、六組都列，沒有事件時也查得到。
    與事件那張圖**共用同一個彈窗**（不另建一份 DOM），但標題分得出來是哪一種——
    速查被當成現況是最容易發生的誤讀。
  - **一種天氣一行、按鈕直接寫「磁暴 A／磁暴 B」**（Owner 2026-08-03）：六顆擠成一排時
    「哪兩顆是同一種天氣」只能靠讀字判斷，分行之後那個分組是版面本身講出來的。
    ⚠️ A／B 這個編號**仍是推定**，所以底下那行「※ 待實測確認」不能拿掉——
    編號一旦看起來像事實，就沒有人會去驗它。
  - **圖例只留職業，不列任務名**（Owner：「顯示職業 ICON 即可」）：要決定的是「我這個職業
    要不要去、去哪一點」，任務名在遊戲的任務板上本來就看得到。名稱仍在 pin 的 title 裡。
  - **每一組只顯示自己那一則開始通告**（Owner 兩次要求：「磁暴 A 只顯示 A 的對話，不要混一起」）。
    對應依 **client 內兩者排列順序相同** 推定（通告第 0 則→α、第 1 則→β）——**這是假設，不是證據**，
    所以旁邊掛「※ 對應待實測確認」。解出來（B-023）之前那個記號不能拿掉：
    這一欄錯了會把人送到地圖的另一半，而錯的形式是完全沒有訊號。
  - 說明文字砍到只剩聊天欄那兩句（Owner：「一堆字幹嘛」）。推理過程留在註解與 ❓ 說明卡。

- **手動通報可以選到變體（α／β）**（Owner：「你要把天氣 AB 的選項分出來啊，說明也要指名
  哪些是 A 哪些是 B，不然報告的人怎麼分」）。原本選單只到「哪一種天氣」，把 α／β 全推給插件——
  但插件只看得到 1～2 台伺服器，其餘 5 台的變體因此**永遠是空的**，而變體正是之後地圖要用來
  指出「哪個職業去哪裡」的那一欄。
  - 選單是**三段確定程度**，不是六選一：`不確定` ／ `磁暴（只看到預告，不確定 A／B）` ／
    `磁暴 A`。**中間那段是必要的**——預告那一則對 A、B 是同一句，只看到它的人分不出來，
    逼他選就是在製造假資料（鐵則 §2）。
  - 收合的原文對照表同步標明哪則是 A、哪則是 B（六則全出自 client 的
    `MassivePcContentTextData`）。選單問 A／B，說明就得答得出 A／B。
  - **後到的附議者只補空欄**（`COALESCE(舊, 新)`）：先看到預告的人報不出變體，事件開始後
    才進來的人報得出 ⇒ 補上；但已經定下來的不給改寫，晚到又選錯的人蓋不掉。
  - 兩欄互相矛盾時（`weather=spore` ＋ `variant=storm-a`）**變體贏**——它是逐字比對通告來的。
  - 事件列上顯示 `磁暴 B`／`磁暴`，**沒填就整個不放**（不寫「未知」——那會被讀成
    「查過了是未知」，但真相是沒人填）。不顯示的話這一欄收得到也沒人看得出填錯了。

- **手動通報靜置 30 秒才推播**（Owner：「30 秒內可取消」）。誤按的人需要時間注意到並按取消；
  30 秒對事件本身幾乎無成本（20 分鐘的 2.5%）。**實測佐證**：那天最近 4 筆通報有 2 筆被撤回
  （id 11、13），而在舊行為下那兩則通知**都已經送到訂閱者的 Discord**，撤回只把網站紀錄下架。
  - **插件通報不延遲**：它回報的是遊戲天氣本身（`ActiveWeather ∈ 194–197`），不存在誤按，
    延遲它等於丟掉它唯一的優勢——天氣翻轉當下就送。兩種來源不混為一談（AGENTS §4）。
  - **用 DO alarm，不是在 `waitUntil` 裡睡 30 秒**。DO 隨時可能被回收，睡到一半通知就消失，
    **而且沒有任何訊號**：沒有錯誤、沒有警告，只是通知沒來。alarm 由平台保證會被叫到。
  - 剛好對上撤回規則：撤回在「已有人附議」後即關閉，而附議來自看到通知的人
    ⇒ 靜置期內撤回窗口一定是暢通的。
  - **事件本身立刻就在網站上**——延遲的只有推播，不是狀態（有測試釘住）。

- **靜置期可以提前結束**（Owner：「30 秒內確定有 也可以直接通告」）。靜置期擋的是
  「沒人確認的孤例」，不是「已經有第二個人看到的事件」：
  - **別人附議 → 自動立刻送**（不需任何操作）。
  - **通報者按「我確定，馬上通知」**——只有本人、只在靜置期內、送過即 403 不重送。
    不削弱保護：誤按的人不會去按它，有惡意的人本來就等得起 30 秒。
  - `/state` 新增 `pendingNotify`：按鈕顯不顯示、撤回時該說「沒有人會收到」還是「無法收回」，
    都由它決定。**前端自己猜會錯**——靜置期以伺服器時鐘計，且附議／插件證實會提前送。

### Changed

- **通報提前量上限 15 → 5 分鐘**，修正 8/2 段落裡「後端上限仍是 15」那個決定。
  當時的理由是「單一觀察不該當硬上限」，但**那個顧慮用錯了地方**：它適用於猜遊戲行為
  （天氣閘就是這樣栽的），這裡則是在描述**通報者能知道什麼**——玩家唯一的資訊來源是那則
  預兆通告，通告只提前約 5 分鐘（實測 5:15／5:40）⇒ 沒有任何管道能知道 10 或 15 分鐘後的事，
  收得比這寬只是收噪音。遊戲若日後改成更早預告，症狀是通報被 400 退回（看得見），不是靜默漏掉。

- **CSP `connect-src` 放行 `discord.com`／`discordapp.com`**（使用者回報「測試發送顯示 Failed to fetch」）。
  portal 設定 modal 的「🧪 測試發送」是從**本頁**直接 POST 到 webhook、不經任何後端，
  少了這兩個來源瀏覽器會在送出前擋掉，症狀看起來像 webhook 設錯或 Discord 掛了。
  本站自己的通知由 worker 送，不受影響。**同型缺陷在 13 個工具裡有 10 個**——`_headers` 是
  複製貼上的範本，sub-timer 當初補的沒有回流，之後每個新工具都從缺 discord 的版本複製。
  **本機永遠測不出來**：`_headers` 只在 CF Pages 上生效。

## 2026-08-02（二）— 上線後：歷史紀錄、不揭露來源、預設伺服器與 settings 競態

### Added

- **記住看的是哪一個分頁**（Owner：「F5 重整不要回到第一個分頁」）。優先序＝**網址 hash** →
  上次看的那頁 → 預設。hash 排前面是因為它是「別人給我的連結」，意圖比我上次的瀏覽狀態明確
  （順帶讓「把緊急事件那頁傳給人」變得可能）。兩者都只認實際存在的分頁 id——從 portal 進來
  可能帶著 `#announce` 之類與本站無關的 hash，那種要安全落回而不是卡在空白。
  切換分頁用 `history.replaceState` 而非 `location.hash =`：後者每切一次就多一筆歷史，
  使用者按上一頁會在分頁之間倒退，而不是回到他真正的上一頁。

- **通報時間可以報「已經開始了 N 分鐘」**（−15／−10／−5）。通報時間原本只可能是「送出的那一刻」，
  發現得晚就整段偏——2026-08-02 實測：真實 17:52 開始、18:02 才被回報，倒數整整偏 10 分鐘，
  而當下**沒有任何辦法修正**。同時把往後的選項砍到 5 分鐘（Owner 實測預告最多約 5 分半，
  留「10 分鐘後」等於邀請人填一個不存在的值），**後端上限當時保留 15**
  （理由見原文；隔天 8/3 收緊為 5，見上一段）。
- **`POST /admin/purge`** — 刪掉已撤銷／已撤回的事件（測試資料與誤按）。歷史表原則是
  「被撤銷的也要列出來」，那是為了**不隱藏真實事件**；但測試噪音不是真實事件，留著只會稀釋
  「緊急事件實際何時發生」這份唯一能累積的資料。**判準用狀態不用時間或 id 範圍**：
  真實事件跑完後狀態仍是 `active`（過期是 lazy 判定、不改狀態），所以刪不到它們。
- **`POST /admin/adjust`** — 校正已存在事件的開始時間，`endAt` 依 20 分鐘常數重算、
  `startExact` 轉 `false`（人工估的值不冒充精確）。上面那個偏差當下無工具可修，這支就是補這個洞。

### Changed（承上）

- **「已經有人回報過」不再呈現成失敗**。後端本來就會自動附議 +1，但兩邊的呈現都像出錯：
  網頁寫「已經有進行中的事件了」、插件把 409 歸在失敗側（狀態行顯示「已送 0 筆」＋一個像錯誤的
  409）。**我自己就是被這個誤導的**——一度以為插件沒送出去，實際上它送到了。
  網頁改成「已幫你附議 +1」，插件把 409 當成成功。

- **預告 → 進行中兩段式**（Owner 2026-08-02：「提示緊急通知的時候就預報，然後真正開始就是事件中」）。
  遊戲的預兆通告走**聊天欄系統訊息**，比天氣翻轉早幾分鐘：
  > 「觀測到孢子霧爆發的預兆！能見度將會受到影響，出於安全考慮，請工作人員們做好準備，依據情況停工。」
  - 插件掛聊天監聽比對關鍵字。**第一版取「爆發的預兆」是錯的**：那句只屬於孢子霧，
    當晚流星雨那次（「觀測到小型隕石群正在靠近！」）完全沒觸發。我在註解裡寫「三種天氣**應該**
    共用同一句」——那個「應該」是拿一個樣本猜的，而猜錯的代價是**靜默漏掉**。
    已改為只比對兩版唯一的共同點 **「觀測到」**，並在命中時把原文與聊天頻道型別寫進 log
    （磁暴那一版還沒見過，之後要收緊比對才有依據，不會再憑一個樣本推規則）。
  - `warn` 建立 `startAt=0` 的事件；天氣真的翻轉時**就地把同一筆升級**成進行中，
    不開新事件——開新的會讓同一起事件在歷史裡變兩筆，提前量就算不出來。
  - **預告階段刻意不顯示倒數**。2026-08-02 只量到一筆提前量（預告 17:23:00 → 實際開始
    17:28:40，**5 分 40 秒**）。拿單一觀察當倒數是這輪已經吃過虧的同型錯誤（天氣閘），
    所以只寫「即將開始」＋預兆出現的時刻，等歷史累積夠了再談要不要顯示數字。
  - 歷史表新增**「預告提前」**欄（`leadSeconds`）——那正是累積這個數字的地方。
  - 這是唯一會對同一筆事件推播兩次的情況，而且是刻意的：預告與開始是兩則不同的資訊。

- **通報者可以撤回自己按錯的那一筆**（`POST /withdraw` ＋ 現況列上的「取消（我按錯了）」）。
  Owner 2026-08-02：「自己按的應該要能取消（避免誤按）」——原本誤按之後只能等三個陌生人
  來否認，太慢也太尷尬。
  - **三個條件缺一不可**：是本人（`reporter` 相符）、事件還在進行中、**而且還沒有人附議**。
    最後一條是關鍵：有人附議代表別人也看到了，那就不是誤按；它同時擋掉
    「先亂報、再撤掉」的反覆騷擾循環——附議一出現撤回權就消失，只能走留痕的否認流程。
  - 狀態用 `withdrawn` 而**不是**沿用管理端的 `revoked`：歷史表要分得出「本人自己收回」
    與「管理端下架」，那是兩件性質完全不同的事。
  - **UI 明說「先前送出的通知無法收回」**——推播是即時的，發出去就追不回來，
    不能讓人以為按了取消就當作沒發生過。
  - 「這筆是不是我按的」由本機 localStorage 記（有上界 20 筆）：`/state` 刻意不回 `reporter`，
    那是別人的識別碼，沒有理由發給所有人。**權限判定仍在後端**，前端只決定按鈕顯不顯示。

- **歷史紀錄**（`GET /history` ＋ 分頁上的表格）。緊急事件是全站**唯一無法重算**的資料：
  其他東西都是時間的純函數，只有「某台伺服器在某個時刻真的出過事件」除非記下來否則永遠找不回。
  原本事件只留 7 天且 `/state` 只回進行中的，等於這個功能沒有出口。
  - **保留期改兩段**：事件列留 **90 天**；結束滿 **7 天**即**去識別**——清掉 `reporter`／
    `confirms`／`disputes` 的 UUID，只留 `nConfirm`／`nDispute` 計數（投票當下同步維護，
    所以清掉陣列不失資訊）。歷史要的是「何時、哪一台、幾個人證實」，不需要知道是誰。
  - **被撤銷的也列出來並標明**。藏起來的話這張表就會變成一份「看起來很乾淨但被修過」的資料。
  - **顯示狀態按時間補算**：DB 的 `status` 不會因為時間到就改（過期是 lazy 判定、刻意不排 alarm），
    直接拿它當標籤會在歷史表裡寫出一整排「進行中」——實測踩到，已修。
  - UI 明講**不要拿它當預測用**：目前沒有任何證據顯示緊急事件有規律。

### Changed

- **不再顯示回報來源**（Owner 2026-08-02：「不要說明用插件偵測，就說通知回報即可」）。
  現況列的徽章改為中性的「已回報」，Discord 訊息與網頁通知也移除來源那一行。
  後端仍記 `source` 供管理端 `stats` 觀察兩種來源的品質——**內部可見、對外不提**。


### Fixed

- **通報表單的預設伺服器不再是「伊弗利特」**。`<select>` 天生選第一個選項 ⇒ 泰坦的玩家
  沒注意到下拉框按下去，送出的是一筆**伊弗利特的假通報**。這種「不是惡意但是錯的」通報，
  社交層完全分辨不出來——只會被三個人否認掉，代價是另一台伺服器的人先被吵一次。
  - 改為：**上次選的** → portal 跨工具的 `character.mainWorld` → **不預設**（顯示「請選擇伺服器」
    且送出鈕停用）。兩者都沒有時寧可逼人選一次，不猜。
  - `character.mainWorld` **只讀不寫**：那是使用者在 portal 設的跨工具身份，不該被這頁的
    臨時選擇覆蓋（Owner 2026-08-02：「portal 設定有我的伺服器，但還是要人進去調整會更好」）。
- **`FFXIVSettings` 競態：填過 webhook 的人被告知「未設定」**。`settings-client` 是非同步
  hydrate 的，訂閱區在建構當下就同步讀 `discord.webhookUrl` ⇒ 讀到空字串，而且不會再刷新。
  2026-08-02 正式站實測踩到：畫面說未設定，實際值是在的。
  - 這個錯誤方向特別糟——它會讓人以為要回設定頁重填，或以為只能收網頁通知。
  - 修法：`load()` 與 `save()` 都先 `await FFXIVSettings.ready`，並掛
    `onChange('discord.webhookUrl')` 讓之後的改動也跟著更新。`save()` 那條尤其重要：
    早按一秒就會把**空的 webhook** 存上去，變成「訂閱成功卻收不到 Discord」。

### Changed

- **訂閱區改成講實話的三段狀態**：未訂閱時預勾 `character.mainWorld` 但明寫
  「已預先勾選 X，按『儲存訂閱』才會生效」——勾好了跟訂閱生效了是兩回事，
  預勾看起來像已訂閱就是騙人。

## 2026-08-02 — 緊急事件改由回報（cycle 2026-08-02-emergency-report）

### Fixed

- **「緊急任務的必要條件是靈風」是錯的，全站文案已更正**。`modules/forecast-view.js` 的靈風視窗卡
  寫「N 個**緊急**任務的必要條件」，但那個 N（＝20）數的是掛 cond 13 的任務，它們的 `class`
  **全部是 `temporary`**（臨時任務）；真正的 33 個 `critical`（緊急）任務 `conds` 欄**全部是空的**。
  - **client 裡沒有任何欄位把緊急任務綁到任何天氣。** 錯的是「掛這條件的是哪一批任務」，
    不是「row 13 是天氣 FK」（後者仍然成立）。
  - 為什麼躲過了：2026-07-31 分類判準修正（`2577acf`）改變了 critical/temporary 的判準，
    但這幾句文案沒跟著改；註解甚至還停在更早的「11 個」，與畫面顯示的 20 已經對不起來
    ——**兩個數字互相矛盾了兩天沒人發現**，因為沒有任何測試在看文案。

### Added

- **「緊急事件」分頁 ＋ `worker/` 後端**：緊急事件（磁暴／流星雨／孢子霧）離線算不出來，
  改由兩種來源回報 —— **ICE 插件自動偵測**（遊戲天氣變成 194–197 就回報，訊號是遊戲自己的狀態）
  與**玩家手動通報**。訂閱者依伺服器收到 Discord（後端送，關機也收得到）與網頁通知
  （分頁開著時）。UI 分別標示兩種來源與附議／否認數。
  - 後端＝Cloudflare Worker ＋**單一** Durable Object（SQLite）。不分片：量體是「每台伺服器
    每小時最多幾筆」，而 fan-out 必須跨伺服器讀訂閱表。不排 alarm（過期 lazy 判定）。
  - Discord fan-out **不重試、不做 outbox**：事件只有 20 分鐘，遲到的通知比沒有更糟。
    失敗累計 4 次熔斷（防燒額度）。SSRF 白名單只放行 `discord.com`／`discordapp.com`。
  - 身份沿用 portal 跨工具 UUID 與 `discord.webhookUrl`，**本站不自建**。webhook 只回遮罩值；
    事件保留 7 天；退訂實體刪列。

### Changed

- **`AGENTS.md` 鐵則 §4 改寫**：「本站不提供緊急事件」→「演算法這一半不變（不預測、不排程、
  不猜下一次），另一半改由回報，且天花板要寫在 UI 上」。
- **`docs/BACKLOG.md` B-006 結案**：Owner 2026-07-31 的「不要有人主動偵測」由 2026-08-02 的
  「插件跟人工通報都做」取代。

### Removed

- **天氣閘（沒有進到程式碼就被砍掉）**。原設計要拿「通報當下是不是靈風／月塵」當反惡意閘，
  依據是 Owner 2026-07-31 的遊戲內觀察。動工前拿 ICE `board-log.jsonl` 驗證，**當場證偽**：
  - 該檔 186 筆涵蓋 3.9 小時，其中 **15 筆 `weather=196`（緊急天氣）**，
    同一時段板上出現 5 個 critical 任務（518/522/530/537/543）⇒ 確實是一次真實事件。
  - 那 15 筆的**底層演算法天氣 15/15 都是「晴朗」**，不是靈風也不是月塵。
  - 同一份記錄裡非緊急的 171 筆，演算法與遊戲實測 **171/171 全吻合** ⇒ 不是演算法或記錄的問題。
  - ⇒ **緊急事件會在晴朗時段發動**，天氣閘會把真實通報靜默退掉。**一筆陽性觀察即足以否證**
    （與「條件開了卻沒出現」那種可被 Lottery 解釋的陰性觀察不對稱，見 `tools/compare-board-log.mjs`）。
  - **教訓**：這是本 repo 第三次遇到「數字看起來很合理但是錯的」（前兩次＝`col[14]/col[15]`、
    refin join）。差別在**這次在寫任何程式碼之前就驗了**，成本是一支 20 行的分析腳本。
    可驗證的假設就去驗，不要先蓋在上面。
- **Service Worker 背景輪詢（同樣沒進到程式碼）**。原規劃「關掉分頁、瀏覽器還開著就會響」，
  查證後放棄：SW 在沒有分頁時會被瀏覽器終止、計時器不會叫醒它
  （[W3C service-worker lifetime](https://www.w3.org/TR/service-workers/#service-worker-lifetime)）。
  改成頁內輪詢，並在 UI 明說「網頁通知只在分頁開著時有效；要關掉也收得到就用 Discord」。
  **與其做一個時靈時不靈的東西，不如老實說它的範圍。**

## 2026-08-01 — 需求物接上製作求解器＋移除錯的三段門檻 join（cycle 2026-08-01-recipe-quality-stages）

### Removed

- **`_unverified.evalThresholds` 整個拿掉——那是錯的 join**。原本用
  `refinSheet.TryGetRow(unit.RowId)`，**拿任務 id 當列號**去查 `WKSMissionToDoEvalutionRefin`。
  真鍵是**配方**的 `Recipe.CollectableMetadata`（且要 `CollectableMetadataKey == 7` 才指這張表）。
  該表 798 列、不是按任務 id 編的。
  - 為什麼一直沒被發現：兩邊值域剛好重疊（都落在 1–791）⇒ 543/544 筆都查得到、看起來完全正常；
    798 列裡只有 12 種相異組合、最常見的 50/60/85 佔三分之一 ⇒ 錯的 join 還有可觀比例會意外對上
    （實測任務 #27 用錯的第 27 列與對的第 16 列**剛好同值**）。**零錯誤訊號 + 正確率高到不會被質疑**
    ——與 `col[14]/col[15]` 完全同型。
  - 而且這件事本來就不歸本站管：品質門檻是**配方**的屬性（一般收藏品也有同型欄位）。權威已移到
    monorepo `game_ref.sqlite` 的 `recipe_quality_stages`，消費端是 crafter。

### Changed

- **`recipeIds` 從 `_unverified` 扶正為正式欄位 `recipes`**（`Recipe` row id 陣列，最多 5 個）。
  內容與舊值逐筆一致，400 個製作任務有值。`_unverified` 這個容器現在整個空了 ⇒ 一併移除。

### Added

- **需求物直接連到製作求解器**（`modules/crafter-link.js`）。任務清單的「需求物」欄，製作類任務
  每個需求物都是連結 → `ffxiv-crafter/?item=<itemId>`；採集／釣魚類沒有配方，維持純文字。
  - 用 `?item=` 而非 `?recipe=`：本站的權威是「要交什麼物品」，配方 id 讓 crafter 自己解。
    實測不變量：400 個製作任務的需求物**全部**在 crafter 配方表查得到。
  - **刻意不帶目標品質**：換算實作只有 crafter 一份，本站塞絕對數字進去等於開第二條換算路徑。
  - 主面板那一列整個是 `<button>`（點了跳任務清單），不能巢狀 `<a>` ⇒ 連結只放任務清單。
- **`validate.mjs` 補 4 條不變量**：有配方的任務 400 筆／全落在製作職／配方陣列不含 0／
  `_unverified` 不得復活。

### 已知缺口（非本輪造成，寫成有數字的不變量追蹤）

- **117 個任務抓不到需求物**，其中 40 個有配方、含全部 16 個雙職業任務。`WKSMissionUnit` 有多個
  ToDo 槽（上游 BestCraft 的 entity 是 `ToDo0/1/2Id`），本站只讀了 `col[11]` 一個。
  數字寫死在 `validate.mjs` ⇒ 修好會讓它紅、逼人回來把數字改小；變多同樣紅。

## 2026-07-31 — 任務分類改用遊戲原文（cycle 2026-07-31-cosmic-taxonomy）

### Changed

- **三個分頁的分界改用 rank，不再用 c18**。原本拿 c18 當「基礎旗標」，交叉表一驗才發現
  c18=1 ⇔ rank≤3 完全等價 ⇒ 那欄沒帶任何獨立資訊，只是把 rank 4（A）誤推到臨時側。
  決定性證據是任務名前綴、零例外：無前綴 352 筆＝D 99／C 77／B 88／**A 88**，【高難】96 全 rank 5，
  【高難+】96 全 rank 6。對上 Owner 的遊戲內回報「基礎任務才有分 D C B A」。
  基礎 231→**319**、臨時 280→**192**、緊急 33 不變。
- **分類名與子標籤全部改用 client 原文**，並記下 Addon row id 供改版後回頭核對：
  基礎任務(16748)／臨時任務(16749)／緊急任務(16750)、連續任務(16871)／時間限定任務(16872)／
  天氣限定任務(16873)。先前的「基礎／臨時／緊急」是猜的簡稱，A1/A2/A3 是上游 ICE 的內部命名。
- **看板改成「必定出現」與「抽選池」兩組**。Owner 遊戲內回報：有條件的任務在條件成立時
  **必定**出現（天氣任務在該天氣期間一定在板上），走抽選的是不限時的那批。
  原本一律講「可能出現」，把兩種不同性質的東西混成同一個數字。此規則 client 內無法佐證，已記 B-009 待核。
- **職業一律以圖示代替文字**（表格欄、篩選 chip、看板卡片、天氣時間軸）。11 職名各 3–4 字，
  攤在表格與 chip 列上會把版面撐爛（Owner 2026-07-31）。語意改由 `alt`／`title`／`aria-label` 承載。

### Added

- **`WKSMissionUnit` c16 定性＝前置任務**，也就是遊戲的「連續任務」。88 筆非零，三項證據同時成立：
  指向的任務同職業 88/88（其餘候選欄 c1 47/544、c17 4/448 差得遠）、名稱語意直接對得上且遊戲自己
  在名字裡寫了「續·」（「【高難】續·製作傳動用的長桿」→ #33「製作傳動用的長桿」）、全部落在 rank 5/6。
  **這就是上游 ICE 一直填 0 的 `LockedBehind`**，已記 B-008 回饋給 fork。
  任務清單的「開放條件」欄現在直接寫出要先完成哪一個——這才是「條件都符合卻看不到」最常見的原因。
- 子標籤篩選（連續／時間限定／天氣限定），以及健全性閘的三條新規則（連續 88、連續任務不得落在
  臨時分頁之外、前置任務不得指向不存在的任務）。

### 驗證

- **ET 時段語意通過遊戲內核對**（B-004 收官）。#445「採集精密空氣淨化裝置所需的材料」（園藝師／A）
  本站解出 ET 10:00–12:00，Owner 在遊戲內看到 10:00~11:59；同名的採掘師版 #400 是 02:00–04:00，
  兩者不同 ⇒ 不是碰巧對上任意 2 小時級距。單位確認為艾奧傑亞小時，
  看板的「⚠ 時段解讀未經遊戲內驗證」警告已撤除。

### 裝了偵測器，猜測時代結束

Owner 一句「ice 能抓到這些資料，為何不直接在插件裡裝個偵測器，要我一直幫你找？」——這是對的，
而且是本輪所有反覆的根源：**我把「離線無法驗證」當成了「只能請 Owner 去看」**，
於是每個問題都要一次單點觀察，而抽選讓單點觀察永遠無法定案，只好一直翻案。

- **ICE fork 新增 `BoardLogger`**（`/ice log on`，預設關、純唯讀）：任務板開著時把
  實際看板內容 ＋ ET ＋ 天氣 ＋ 職業寫成 JSONL。同一個 350 秒格線內同職業同清單不重複寫。
- **新增 `tools/compare-board-log.mjs`**：把記錄跟本站預測對帳，輸出三塊——
  硬反證（板上有、本站說沒開，**一筆就是反證**）／出現率（本站說開時實際出現的比例，
  **要看比率不看單次**）／c14 相關性檢定。
- **第一份記錄（23 筆、園藝師、52 分鐘）當場定案兩件事**：

  ① **抽選是真的**：#437 在條件全部成立的 12 次機會中只出現 5 次（42%）。
  「條件成立＝必定出現」正式出局，全站措辭改為「條件符合」。

  ② **`c14` 非零＝該任務整批拿不到**：實際出現過的任務全部 c14=0（7 次零例外）；
  c14 非零者 **208 次機會零出現**，含 32 次「c14 自己的視窗也開著」。
  ⇒ 不是多一道時段閘，而是另一個未知的解鎖軸。本站標 `blocked`、**不預測、不猜**。
  面板剩下 22 筆可預測的臨時任務——每個職業各一筆靈風、一筆月塵，形態完全規則，
  且與 Owner 靈風時段的實地觀察逐筆吻合。

  硬反證：**0 筆**。

### 階級門是 per-job 的，而且能從記錄自動推出來

Owner 指出「不用切職業，有能力接的就會自己顯示出來」——log 直接佐證（同一筆記錄同時出現
園藝師與烹調師的任務）。這推翻了對帳腳本一個沉默的假設：它一直只評估「當下職業」的任務，
等於丟掉大半訊號、分母也全錯。修掉之後浮出一個零例外的分界：

| 職業 | 已確認解到 | 高難+ 出現/機會 |
|---|---|---|
| 鍛造師・園藝師 | 高難+ | **9/41** |
| 皮革師・裁縫師・烹調師 | 高難 | 0/41 |
| 木工師 | D | 0/41 |

也就是**階級門真實存在，但它是 per-job 的**。站上曾放過一個全域的「我的階級」選擇器，
形狀就不對（Owner 也正確地指出它沒用），已於本輪移除——現在改由記錄自動推導，使用者零設定。

- 新增 `tools/derive-progress.mjs`：從記錄推出每職已確認的最高階，寫成 `data/observed-progress.json`。
  **只做正向斷言**——某階的任務曾上板 ⇒ 該職至少解到那階（硬事實）；沒看到不能反推沒解（抽選）。
  沒有記錄的職業一律**不過濾**，因為對它一無所知，過濾等於憑空猜測。
- 面板據此隱藏超過階級的任務，並在上方明講依據與隱藏筆數——**隱形過濾比不過濾更糟**。
- 對帳腳本的統計單位改成「不同的看板快照」而不是記錄筆數：看板只在重抽時才變，
  用筆數當分母會把一次觀察灌成幾十次。

### 拿掉「出現率」——那個數字是我的取樣偏差，不是遊戲行為

一度從 ICE 記錄算出逐筆任務的「出現率」，顯示在面板上、還拿它藏掉「幾乎不會出現」的任務。
**那是錯的，而且是最糟的一種錯：用一個看起來很嚴謹的統計去誤導。**

分母的算法是「靈風期間的每一個看板快照都算該任務一次機會」，但**一個快照只是當下那個
職業／分頁的清單**——鍛造師的靈風任務不可能出現在園藝師的清單裡，卻被算成「一次沒中」。
量到的是取樣偏差，不是抽選機率。Owner 2026-08-01 直接指正：「天氣出就必出的東西你寫什麼出現率」。

已全部移除：出現率標註、「機會 20 次卻從未出現就不列」的過濾、按出現率排序、
以及全站的「不保證上板／Lottery 抽選」措辭。`derive-progress.mjs` 的逐筆統計也拿掉，
並在檔頭寫死「不要用這份記錄算出現率」與理由，避免以後又被誤用。

現在的說法：**有時段／天候條件的 → 條件到了就「會出現」**；只有不限時的那 392 個
因為看板名額有限會輪替，那批才標「不限時」。保留下來的只有 per-job 階級門——
它只用「看到過某階 ⇒ 至少解到那階」的正向斷言，不涉及分母。

### 拿掉「用我的記錄當篩選依據」——這站是要給別人用的

一度把 ICE 記錄推出的每職階級寫成 `data/observed-progress.json`，讓面板據此隱藏
「超過該職階級」的任務。**設計就錯了**：別人打開這個站沒有這份記錄，而且本站本來就
無從得知任何人每個職業練到哪（Owner 2026-08-01）。

改成語意明確的做法：**勾選「我練的職業」＝把該職當成已解到最高階**，站上補一句說明，
不做任何猜測。`derive-progress.mjs` 降級為純離線分析工具（輸出到 stdout、不落檔），
並在檔頭寫明它不餵網站與原因。

### 版面：功能分頁化

- **天氣預報獨立成一個分頁**，並拿掉時間軸的「天候任務」欄（職業圖示把表格擠爆，
  也把兩件事混在一起）。現在三個分頁各答一個問題：臨時任務（現在能做什麼）／
  天氣預報（什麼時候會是什麼天氣）／任務清單（全部 544 筆）。
- **任務清單的篩選欄與表格欄同序**：職業 → 任務類型（含子標籤）→ 難度 → 開放條件 → 搜尋 → 開關。
  原本搜尋擺最前、職業被夾在類型與難度中間，跟表格對不起來。
- **任務清單欄序重排**成「誰 → 是什麼 → 何時 → 代價 → 報酬」：
  職業｜類型｜難度｜任務｜開放條件｜現在｜時限｜需求物｜宇宙｜月球，與主面板同一套邏輯。
  獎勵也拆成宇宙／月球兩欄，跟面板一致。
- 踩到一個拆分頁的坑：`forecast-view` 用 panel root 查 `#fc-timeline`，時間軸搬到新分頁後
  變成 null、整片消失且只在 console 出現例外。改用 document 查，並在註解寫明原因。

### Fixed

- ~~**前提錯了：`c14` 根本不是條件欄，條件只在 `c15`。**~~ **（此結論已被實測推翻，見上）** 這是本輪最大的一次修正，
  連帶讓先前四次「AND 還是 OR」的翻案全部作廢——那四次都是**在錯誤前提上做推理**。
  決定性證據是 Owner 的一句話：「ice 能抓到這些資料，為何不直接在插件裡裝偵測器」——
  照著去翻 ICE fork，發現 `PlayerHandlers.SinusMapV2` 是上游**依遊戲內觀察手工建的**
  「ET 時段 → 任務」表。拿它對帳：**22/22 全吻合 c15，零筆對到 c14**，
  而且每一筆雙槽任務都被列在 c15 的時段（#448 在 ET 22–24，不是 c14 的 ET 00–02）。
  加上分布形狀（c15 十二窗各 1–3 筆、靈風 20 月塵 21；c14 缺兩個窗、只有靈風沒月塵）
  與 Owner 三次實測，三方一致。有條件任務 152 → **63**（ET 22 ＋ 靈風 20 ＋ 月塵 21）。
  `c14` 的真實語意仍未知，**刻意不猜**，只保留一個讀取點供日後查核。
  健全性閘新增：條件數必為 63、單一任務不得有多個條件、ET 條件任務必為 22（對照 SinusMapV2）。
  教訓：**當一個模型需要反覆特例化才兜得住觀察，該懷疑的是輸入欄位選錯了，不是繼續調規則。**

- **雙條件槽定案為純 AND**（B-011 收官）。這條被推翻三次才收斂：
  ① 一律 AND（無證據，只是保守）→ ② 一律 OR（因為 11 筆兩槽都是互斥 ET 時段，純 AND
  不可滿足——但這是**從「資料應該合理」反推**，不是觀察）→ ③ 跨類型 AND（Owner 靈風時段實測，
  ET＋天候的 3 筆在天候開著、ET 未到時都不在板上）→ ④ 同類型也是 AND：Owner 在
  **ET 23:00 用園藝師看不到** #448（ET 00:00–02:00 ＋ ET 22:00–24:00），OR 之下它該在板上。
  代價是那 11 筆兩槽皆 ET 的永遠不可滿足 ⇒ 一律判「無法判定」、主面板不列，
  **既不宣稱可做也不宣稱永遠不可能**（B-012）。
  教訓：這四次裡有兩次是我從「資料應該長得合理」反推，而不是等實測——凡是這樣得到的結論
  都該標成假設，不該直接寫進判定邏輯。
- **緊急任務不再列進主面板**。它的天候條件只是**必要**條件，還要等伺服器推播事件——
  上面那 8 個誤報裡有 3 個就是緊急任務（製作火焰噴射器的零件／調查孢子霧對植被的影響／採集有益菌類）。
- **雙職業任務的排版**。兩個職業圖示各自佔掉一個 grid 欄，把後面每一欄都往右推一格。
- **加上欄位標題**，並用 `rem` 而不是 `em` 定義欄寬——標題列是 `.codex-small`、資料列是預設字級，
  `em` 會各自對自己的 font-size 解析，實測差了 46px。
- **天氣卡直接寫出下一段天氣**（`還剩 22 分 12 秒 → 接著是 🌤 晴朗`）。只講「還剩多久」是半句話，
  變完之後是什麼才是要不要現在收工的依據。晴朗接晴朗時再補「距離下一個特殊天氣」。
- **標題右邊標明範圍**（「只顯示你勾選的職業：鍛造師、園藝師」）。沒有它，使用者會把
  「現在有 3 個」當成全服只有 3 個。
- **移除「我的階級」選擇器**（Owner 2026-07-31）。它只在還沒解到【高難】時有作用，
  解開之後永遠不過濾任何東西，等於常駐一個沒用的控制項。
- **宇宙工具／基地進度分頁暫時隱藏**（Owner：「目前好像也沒什麼用」）。面板與資料都留著，
  拿掉 `hidden` 即可復原——不刪，因為工具升級鏈是原始需求之一。
- **連續任務改顯示整條鏈，並標明源頭是哪一類**。Owner 2026-07-31 指出「某些臨時任務的
  前置條件應該是完成普通任務」——資料完全吻合：88 個前置裡 **33 個指向 rank A 的基礎任務**、
  33 個指向【高難】、22 個指向【高難+】，鏈形是 `基礎 A →【高難】續·X →【高難+】X`。
  原本只寫「先完成『X』」，看不出 X 是哪一類、也看不出還有幾層 ⇒ 改成
  `先完成基礎任務「製作貨物包裝材料」→「【高難】續·製作貨物包裝材料」`。
  知道源頭在基礎分頁，才知道該從哪裡開始做。
- **16 個雙職業任務移出主面板**。Owner 實測：ET 12–14 且靈風時，本站列出「【高難+】製造高火力爐」
  （園藝＋甲冑）但遊戲內沒有，同時刻的兩個單職業任務都在。先猜「第二職沒練」，**被 Owner 直接否證**
  （兩個職業都有，仍看不到）。資料上它們自成一區：`JobCategorySecondary` 非零者恰好等於
  `c21 ∈ {20000, 20100}`（交集 16/16、id 連續 #496–511），且是全表唯一沒有 rank-4 對應版本的一組。
  出現規則未知 ⇒ **不放進主清單假裝算得出來**，只在面板底部說明有這 16 筆。記 B-013。
- **緊急事件告誡：只在特殊天氣進行中才顯示**。Owner 實測：靈風進行中突然轉孢子霧，
  限時任務整批消失、改出緊急事件。緊急天氣會**覆蓋**原本的天候，而那三種天氣不在任何
  機率表裡＝伺服器推播，本站既無法預測也無法偵測 ⇒ 這段時間上面的天氣限定任務全部不準。
  晴朗時本來就沒有天氣限定任務，常駐這段只會變成沒人讀的背景雜訊 ⇒ 條件顯示。記 B-014。
- **條件欄改顯示實際的值**（`ET 12:00–14:00`／`天氣：靈風`／`ET 00:00–02:00 或 ET 22:00–24:00`），
  不再只寫「時間限定任務」這種類別名——類別名看了還是得再點進去才知道幾點。
- **「下一批」改算「所有條件真正同時成立的下一刻」**。原本只看時段、忽略天候，於是會列出
  下一段天氣根本不對的任務——天候是純時間函數、算得出來，列一個不可能發生的東西沒有價值
  （Owner 2026-07-31）。改用 350 秒（＝2 艾奧傑亞小時）為步長向前掃：ET 視窗邊界正好是它的倍數、
  天候週期 1400 秒是它的 4 倍 ⇒ 涵蓋兩種條件的所有狀態變化，不會跳過開窗。掃描上限 4 天，
  結果依 350 秒區塊快取（每秒重畫但每 350 秒才重算）。順帶修掉先前「下一批 — 0 秒後」的 bug
  （取到還在進行中的天候視窗）。
- **獎勵拆成逐格對齊的欄**（宇宙／月球／Ⅰ Ⅱ Ⅲ Ⅳ）。擠成一串 `30/33　Ⅳ23 Ⅲ15 Ⅱ15`
  沒辦法上下比大小，而「哪個任務比較划算」正是看這欄的唯一理由。
  **欄數由資料推**（台服目前只到 Ⅳ），標題與資料列吃同一份清單 ⇒ 不會錯位。
- **有條件的基礎任務進看板**。舊的 `class !== 'basic'` 把它們全濾掉，於是 ET 16:00–18:00
  唯一開著的「製作貨物包裝材料」（基礎/A/皮革師）在看板上完全不存在。
  排除的改成只有「不限時的基礎任務」302 個。
- **主畫面砍掉重做成兩段清單**（`job-board-view.js` → `now-panel.js`）。
  職業看板一張卡片塞了必定／抽選／連續三個數字＋倒數＋緊急註記，看的人得先讀懂四個概念
  才知道要不要去玩（Owner 2026-07-31：「不要那麼複雜了」）。現在只有：
  「現在有 N 個限時任務」＋逐列（職業圖示｜條件種類｜任務名｜需先完成前置），
  以及「下一批 — M 後」的同格式清單。只列有時段／天候條件的 152 個，
  不限時的 392 個隨時都在、去任務清單分頁看。同時刪掉四道門的說明牆與抽選的定調段落。
  過程中修掉的一致性 bug：看板把「有前置的連續任務」算進抽選池、清單卻正確排除，
  於是卡片寫「抽選 9」點進去只有 6 筆。
- **子標籤只掛臨時任務**（Owner 依遊戲 UI 指正）。基礎與緊急即使有時段／天候條件也不掛，
  條件本身照樣列在「開放條件」欄。時間限定 113→96、天氣限定 52→41，連續 88 不變。
- **從看板點進清單時預設帶好篩選**：「只看條件成立的」預設開，並新增可見開關
  「隱藏不限時的基礎任務（302）」預設開——否則點任一職業進去，前幾十列全是 D 階的
  「在抽選池中」，卡片上算給你的那筆反而被埋掉。底部的基礎任務入口會把兩個開關都關掉。

### 這輪的教訓

四次把「我讀得到的欄位」當成「全部的條件」——緊急任務、階級門、第二個條件槽（c15）、前置鏈，
每次都是 Owner 在遊戲裡撞到才發現。這輪的根因是**沒去找遊戲自己的說法就自訂分類**：
Addon 表裡三個分頁名與三個子標籤一直都在，掃一次關鍵字就有。
往後對這批 sheet 的任何分類斷言，先問「遊戲的介面字串怎麼寫」，再談欄位反解。

## 2026-07-31 — 建站（cycle [2026-07-31-cosmic-site](docs/specs/2026-07-31-cosmic-site-design.md)）

### Added

- **`tools/cosmic-dump`（C#／Lumina）** — 從台服 client sqpack 產生 `data/weather.json`、`missions.json`、
  `cosmic-tools.json`。內建健全性閘（544 任務／天氣機率總和 100%／11 條 ×9 階工具鏈），**任一不過就整批不寫檔**
  ——分次落地會留下「舊檔／新檔／缺檔」三種狀態，而網站是逐檔讀的，等於靜默混用兩個 client 版本的資料。
- **三分頁靜態站** — 天氣時間軸／任務清單（544 筆，可依職業‧難度‧條件‧「現在符合條件」篩選）／宇宙工具升級鏈。
- DEVLOOP 工件、`AGENTS.md` 鐵則、portal 設計系統接線（accent cyan、`cos-` 私有前綴、`.codex-*` 零覆寫）。

### 為什麼是這個範圍

原始需求是「自動偵測各伺服器緊急任務與天氣任務的時間」。動工前先做離線查證，結論把範圍砍成兩半：

**一般天氣（月塵／晴朗／靈風）是 unix 時間的純函數**，全 7 個繁中服伺服器同步 ⇒ 可推算到任意未來，
而且「各伺服器」這個維度根本不存在。演算法正確性用中薩納蘭與摩杜納回推、與既有 sightseeing 的手打表逐項比對確認。

**緊急事件天氣（磁暴／流星雨／孢子霧，Weather#194–197）不出現在 client 全部 172 張 `WeatherRate` 的任何一列**
⇒ 時間演算法永遠擲不出來 ⇒ 只能由伺服器推播，離線零資訊。這是決定性證據，不是推測，所以本站**不提供也不假裝提供**，
改為給出唯一算得出來的線索：11 個緊急任務的必要條件視窗（靈風時段），並在頁面上把理由講明白。

### 刻意沒做的事

- **未定性欄位不填合理值**：`WKSMissionLotterySpecialCond` row 15–22 在台服 client 三欄全 0（上游國際服當它們是
  Clouds/Rain，但渴望灣天氣表根本沒這兩種天氣）⇒ 標 `unknown`、可接欄顯示「未知」。評價門檻與未實裝的工具階數同理。
  **由來是 ICE fork 那輪連續五次拿 0 當佔位**（`MapPosition` 讓採集完全不執行、`missionText` 讓技能完全不放）——
  0 在這批 sheet 裡是有意義的哨兵值；在網站上的同型錯誤形式是「把 6 個緊急任務標成隨時可接」。
- **欄位索引不複製第二份**：三張任務表的欄位索引由 csproj 直接編譯 ICE fork 的唯一定義。產生器本來就只能在
  有遊戲 client 的本機跑，依賴本機路徑可接受；抄一份的代價是台服改版後兩邊漂移，症狀是「數字合理但錯」。

### 順帶發現

- `ffxiv-tw-sightseeing/modules/weather.js` 的**月夜峰機率表是錯的**（手打表把 client 的「晴朗 70」拆成
  Fair 30 ＋ Clear 40）。已記 `docs/BACKLOG.md` B-001。
- portal 的 `.codex-empty` 設了 `display:flex` 但**不在** header.css 的集中 `[hidden]` 守衛名單內 ⇒
  帶 `hidden` 的空狀態照樣顯示。本站用自有 class `.cos-hideable[hidden]` 繞過，已記 B-002 提報 portal。
- `WKSMissionToDoEvalutionRefin`（544 列 × 3 欄）是 ICE fork `TcCapability` 仍停用的「銅星門檻」的候選——
  那邊掃了三張表沒找到，因為它在第四張。已記 B-005。

### 驗證

產生器健全性閘全過；瀏覽器 1920×1080 實測三分頁皆正常、console 零 error、水平溢出 0、
`.page-header` top=64／高 51px（生態基準值）；天候篩選實測 11 筆全為緊急且靈風時段內全標「可接」。
