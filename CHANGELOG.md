# Changelog

> 日期段落制（cycle 收官為段）；條目含人話「為什麼」，不從 git log 自動生成。格式見 DEVLOOP §4.3。

## 2026-08-01 — 需求物接上製作求解器＋移除錯的三段門檻 join（cycle 2026-08-01-recipe-quality-stages）

### Removed

- **`_unverified.evalThresholds` 整個拿掉——那是錯的 join**。原本用
  `refinSheet.TryGetRow(unit.RowId)`，**拿任務 id 當列號**去查 `WKSMissionToDoEvalutionRefin`。
  真鍵是**配方**的 `Recipe.CollectableMetadata`（且要 `CollectableMetadataKey == 7` 才指這張表）。
  該表 798 列、不是按任務 id 編的。
  - 為什麼一直沒被發現：兩邊值域剛好重疊（都落在 1–791）⇒ 543/544 筆都查得到、看起來完全正常；
    798 列裡只有 12 種相異組合、最常見的 50/60/85 佔三分之一 ⇒ 錯的 join 還有可觀比例會意外對上
    （實測任務 #27 用錯的第 27 列與對的第 16 列**剛好同值**）。**零錯誤訊號 + 正確率高到不會被質疑**
    ——與 `col[14]/col[15]` 完全同型。
  - 而且這件事本來就不歸本站管：品質門檻是**配方**的屬性（一般收藏品也有同型欄位）。權威已移到
    monorepo `game_ref.sqlite` 的 `recipe_quality_stages`，消費端是 crafter。

### Changed

- **`recipeIds` 從 `_unverified` 扶正為正式欄位 `recipes`**（`Recipe` row id 陣列，最多 5 個）。
  內容與舊值逐筆一致，400 個製作任務有值。`_unverified` 這個容器現在整個空了 ⇒ 一併移除。

### Added

- **需求物直接連到製作求解器**（`modules/crafter-link.js`）。任務清單的「需求物」欄，製作類任務
  每個需求物都是連結 → `ffxiv-crafter/?item=<itemId>`；採集／釣魚類沒有配方，維持純文字。
  - 用 `?item=` 而非 `?recipe=`：本站的權威是「要交什麼物品」，配方 id 讓 crafter 自己解。
    實測不變量：400 個製作任務的需求物**全部**在 crafter 配方表查得到。
  - **刻意不帶目標品質**：換算實作只有 crafter 一份，本站塞絕對數字進去等於開第二條換算路徑。
  - 主面板那一列整個是 `<button>`（點了跳任務清單），不能巢狀 `<a>` ⇒ 連結只放任務清單。
- **`validate.mjs` 補 4 條不變量**：有配方的任務 400 筆／全落在製作職／配方陣列不含 0／
  `_unverified` 不得復活。

### 已知缺口（非本輪造成，寫成有數字的不變量追蹤）

- **117 個任務抓不到需求物**，其中 40 個有配方、含全部 16 個雙職業任務。`WKSMissionUnit` 有多個
  ToDo 槽（上游 BestCraft 的 entity 是 `ToDo0/1/2Id`），本站只讀了 `col[11]` 一個。
  數字寫死在 `validate.mjs` ⇒ 修好會讓它紅、逼人回來把數字改小；變多同樣紅。

## 2026-07-31 — 任務分類改用遊戲原文（cycle 2026-07-31-cosmic-taxonomy）

### Changed

- **三個分頁的分界改用 rank，不再用 c18**。原本拿 c18 當「基礎旗標」，交叉表一驗才發現
  c18=1 ⇔ rank≤3 完全等價 ⇒ 那欄沒帶任何獨立資訊，只是把 rank 4（A）誤推到臨時側。
  決定性證據是任務名前綴、零例外：無前綴 352 筆＝D 99／C 77／B 88／**A 88**，【高難】96 全 rank 5，
  【高難+】96 全 rank 6。對上 Owner 的遊戲內回報「基礎任務才有分 D C B A」。
  基礎 231→**319**、臨時 280→**192**、緊急 33 不變。
- **分類名與子標籤全部改用 client 原文**，並記下 Addon row id 供改版後回頭核對：
  基礎任務(16748)／臨時任務(16749)／緊急任務(16750)、連續任務(16871)／時間限定任務(16872)／
  天氣限定任務(16873)。先前的「基礎／臨時／緊急」是猜的簡稱，A1/A2/A3 是上游 ICE 的內部命名。
- **看板改成「必定出現」與「抽選池」兩組**。Owner 遊戲內回報：有條件的任務在條件成立時
  **必定**出現（天氣任務在該天氣期間一定在板上），走抽選的是不限時的那批。
  原本一律講「可能出現」，把兩種不同性質的東西混成同一個數字。此規則 client 內無法佐證，已記 B-009 待核。
- **職業一律以圖示代替文字**（表格欄、篩選 chip、看板卡片、天氣時間軸）。11 職名各 3–4 字，
  攤在表格與 chip 列上會把版面撐爛（Owner 2026-07-31）。語意改由 `alt`／`title`／`aria-label` 承載。

### Added

- **`WKSMissionUnit` c16 定性＝前置任務**，也就是遊戲的「連續任務」。88 筆非零，三項證據同時成立：
  指向的任務同職業 88/88（其餘候選欄 c1 47/544、c17 4/448 差得遠）、名稱語意直接對得上且遊戲自己
  在名字裡寫了「續·」（「【高難】續·製作傳動用的長桿」→ #33「製作傳動用的長桿」）、全部落在 rank 5/6。
  **這就是上游 ICE 一直填 0 的 `LockedBehind`**，已記 B-008 回饋給 fork。
  任務清單的「開放條件」欄現在直接寫出要先完成哪一個——這才是「條件都符合卻看不到」最常見的原因。
- 子標籤篩選（連續／時間限定／天氣限定），以及健全性閘的三條新規則（連續 88、連續任務不得落在
  臨時分頁之外、前置任務不得指向不存在的任務）。

### 驗證

- **ET 時段語意通過遊戲內核對**（B-004 收官）。#445「採集精密空氣淨化裝置所需的材料」（園藝師／A）
  本站解出 ET 10:00–12:00，Owner 在遊戲內看到 10:00~11:59；同名的採掘師版 #400 是 02:00–04:00，
  兩者不同 ⇒ 不是碰巧對上任意 2 小時級距。單位確認為艾歐澤亞小時，
  看板的「⚠ 時段解讀未經遊戲內驗證」警告已撤除。

### 裝了偵測器，猜測時代結束

Owner 一句「ice 能抓到這些資料，為何不直接在插件裡裝個偵測器，要我一直幫你找？」——這是對的，
而且是本輪所有反覆的根源：**我把「離線無法驗證」當成了「只能請 Owner 去看」**，
於是每個問題都要一次單點觀察，而抽選讓單點觀察永遠無法定案，只好一直翻案。

- **ICE fork 新增 `BoardLogger`**（`/ice log on`，預設關、純唯讀）：任務板開著時把
  實際看板內容 ＋ ET ＋ 天氣 ＋ 職業寫成 JSONL。同一個 350 秒格線內同職業同清單不重複寫。
- **新增 `tools/compare-board-log.mjs`**：把記錄跟本站預測對帳，輸出三塊——
  硬反證（板上有、本站說沒開，**一筆就是反證**）／出現率（本站說開時實際出現的比例，
  **要看比率不看單次**）／c14 相關性檢定。
- **第一份記錄（23 筆、園藝師、52 分鐘）當場定案兩件事**：

  ① **抽選是真的**：#437 在條件全部成立的 12 次機會中只出現 5 次（42%）。
  「條件成立＝必定出現」正式出局，全站措辭改為「條件符合」。

  ② **`c14` 非零＝該任務整批拿不到**：實際出現過的任務全部 c14=0（7 次零例外）；
  c14 非零者 **208 次機會零出現**，含 32 次「c14 自己的視窗也開著」。
  ⇒ 不是多一道時段閘，而是另一個未知的解鎖軸。本站標 `blocked`、**不預測、不猜**。
  面板剩下 22 筆可預測的臨時任務——每個職業各一筆靈風、一筆月塵，形態完全規則，
  且與 Owner 靈風時段的實地觀察逐筆吻合。

  硬反證：**0 筆**。

### 階級門是 per-job 的，而且能從記錄自動推出來

Owner 指出「不用切職業，有能力接的就會自己顯示出來」——log 直接佐證（同一筆記錄同時出現
園藝師與烹調師的任務）。這推翻了對帳腳本一個沉默的假設：它一直只評估「當下職業」的任務，
等於丟掉大半訊號、分母也全錯。修掉之後浮出一個零例外的分界：

| 職業 | 已確認解到 | 高難+ 出現/機會 |
|---|---|---|
| 鍛造師・園藝師 | 高難+ | **9/41** |
| 皮革師・裁縫師・烹調師 | 高難 | 0/41 |
| 木工師 | D | 0/41 |

也就是**階級門真實存在，但它是 per-job 的**。站上曾放過一個全域的「我的階級」選擇器，
形狀就不對（Owner 也正確地指出它沒用），已於本輪移除——現在改由記錄自動推導，使用者零設定。

- 新增 `tools/derive-progress.mjs`：從記錄推出每職已確認的最高階，寫成 `data/observed-progress.json`。
  **只做正向斷言**——某階的任務曾上板 ⇒ 該職至少解到那階（硬事實）；沒看到不能反推沒解（抽選）。
  沒有記錄的職業一律**不過濾**，因為對它一無所知，過濾等於憑空猜測。
- 面板據此隱藏超過階級的任務，並在上方明講依據與隱藏筆數——**隱形過濾比不過濾更糟**。
- 對帳腳本的統計單位改成「不同的看板快照」而不是記錄筆數：看板只在重抽時才變，
  用筆數當分母會把一次觀察灌成幾十次。

### 拿掉「出現率」——那個數字是我的取樣偏差，不是遊戲行為

一度從 ICE 記錄算出逐筆任務的「出現率」，顯示在面板上、還拿它藏掉「幾乎不會出現」的任務。
**那是錯的，而且是最糟的一種錯：用一個看起來很嚴謹的統計去誤導。**

分母的算法是「靈風期間的每一個看板快照都算該任務一次機會」，但**一個快照只是當下那個
職業／分頁的清單**——鍛造師的靈風任務不可能出現在園藝師的清單裡，卻被算成「一次沒中」。
量到的是取樣偏差，不是抽選機率。Owner 2026-08-01 直接指正：「天氣出就必出的東西你寫什麼出現率」。

已全部移除：出現率標註、「機會 20 次卻從未出現就不列」的過濾、按出現率排序、
以及全站的「不保證上板／Lottery 抽選」措辭。`derive-progress.mjs` 的逐筆統計也拿掉，
並在檔頭寫死「不要用這份記錄算出現率」與理由，避免以後又被誤用。

現在的說法：**有時段／天候條件的 → 條件到了就「會出現」**；只有不限時的那 392 個
因為看板名額有限會輪替，那批才標「不限時」。保留下來的只有 per-job 階級門——
它只用「看到過某階 ⇒ 至少解到那階」的正向斷言，不涉及分母。

### 拿掉「用我的記錄當篩選依據」——這站是要給別人用的

一度把 ICE 記錄推出的每職階級寫成 `data/observed-progress.json`，讓面板據此隱藏
「超過該職階級」的任務。**設計就錯了**：別人打開這個站沒有這份記錄，而且本站本來就
無從得知任何人每個職業練到哪（Owner 2026-08-01）。

改成語意明確的做法：**勾選「我練的職業」＝把該職當成已解到最高階**，站上補一句說明，
不做任何猜測。`derive-progress.mjs` 降級為純離線分析工具（輸出到 stdout、不落檔），
並在檔頭寫明它不餵網站與原因。

### 版面：功能分頁化

- **天氣預報獨立成一個分頁**，並拿掉時間軸的「天候任務」欄（職業圖示把表格擠爆，
  也把兩件事混在一起）。現在三個分頁各答一個問題：臨時任務（現在能做什麼）／
  天氣預報（什麼時候會是什麼天氣）／任務清單（全部 544 筆）。
- **任務清單的篩選欄與表格欄同序**：職業 → 任務類型（含子標籤）→ 難度 → 開放條件 → 搜尋 → 開關。
  原本搜尋擺最前、職業被夾在類型與難度中間，跟表格對不起來。
- **任務清單欄序重排**成「誰 → 是什麼 → 何時 → 代價 → 報酬」：
  職業｜類型｜難度｜任務｜開放條件｜現在｜時限｜需求物｜宇宙｜月球，與主面板同一套邏輯。
  獎勵也拆成宇宙／月球兩欄，跟面板一致。
- 踩到一個拆分頁的坑：`forecast-view` 用 panel root 查 `#fc-timeline`，時間軸搬到新分頁後
  變成 null、整片消失且只在 console 出現例外。改用 document 查，並在註解寫明原因。

### Fixed

- ~~**前提錯了：`c14` 根本不是條件欄，條件只在 `c15`。**~~ **（此結論已被實測推翻，見上）** 這是本輪最大的一次修正，
  連帶讓先前四次「AND 還是 OR」的翻案全部作廢——那四次都是**在錯誤前提上做推理**。
  決定性證據是 Owner 的一句話：「ice 能抓到這些資料，為何不直接在插件裡裝偵測器」——
  照著去翻 ICE fork，發現 `PlayerHandlers.SinusMapV2` 是上游**依遊戲內觀察手工建的**
  「ET 時段 → 任務」表。拿它對帳：**22/22 全吻合 c15，零筆對到 c14**，
  而且每一筆雙槽任務都被列在 c15 的時段（#448 在 ET 22–24，不是 c14 的 ET 00–02）。
  加上分布形狀（c15 十二窗各 1–3 筆、靈風 20 月塵 21；c14 缺兩個窗、只有靈風沒月塵）
  與 Owner 三次實測，三方一致。有條件任務 152 → **63**（ET 22 ＋ 靈風 20 ＋ 月塵 21）。
  `c14` 的真實語意仍未知，**刻意不猜**，只保留一個讀取點供日後查核。
  健全性閘新增：條件數必為 63、單一任務不得有多個條件、ET 條件任務必為 22（對照 SinusMapV2）。
  教訓：**當一個模型需要反覆特例化才兜得住觀察，該懷疑的是輸入欄位選錯了，不是繼續調規則。**

- **雙條件槽定案為純 AND**（B-011 收官）。這條被推翻三次才收斂：
  ① 一律 AND（無證據，只是保守）→ ② 一律 OR（因為 11 筆兩槽都是互斥 ET 時段，純 AND
  不可滿足——但這是**從「資料應該合理」反推**，不是觀察）→ ③ 跨類型 AND（Owner 靈風時段實測，
  ET＋天候的 3 筆在天候開著、ET 未到時都不在板上）→ ④ 同類型也是 AND：Owner 在
  **ET 23:00 用園藝師看不到** #448（ET 00:00–02:00 ＋ ET 22:00–24:00），OR 之下它該在板上。
  代價是那 11 筆兩槽皆 ET 的永遠不可滿足 ⇒ 一律判「無法判定」、主面板不列，
  **既不宣稱可做也不宣稱永遠不可能**（B-012）。
  教訓：這四次裡有兩次是我從「資料應該長得合理」反推，而不是等實測——凡是這樣得到的結論
  都該標成假設，不該直接寫進判定邏輯。
- **緊急任務不再列進主面板**。它的天候條件只是**必要**條件，還要等伺服器推播事件——
  上面那 8 個誤報裡有 3 個就是緊急任務（製作火焰噴射器的零件／調查孢子霧對植被的影響／採集有益菌類）。
- **雙職業任務的排版**。兩個職業圖示各自佔掉一個 grid 欄，把後面每一欄都往右推一格。
- **加上欄位標題**，並用 `rem` 而不是 `em` 定義欄寬——標題列是 `.codex-small`、資料列是預設字級，
  `em` 會各自對自己的 font-size 解析，實測差了 46px。
- **天氣卡直接寫出下一段天氣**（`還剩 22 分 12 秒 → 接著是 🌤 晴朗`）。只講「還剩多久」是半句話，
  變完之後是什麼才是要不要現在收工的依據。晴朗接晴朗時再補「距離下一個特殊天氣」。
- **標題右邊標明範圍**（「只顯示你勾選的職業：鍛造師、園藝師」）。沒有它，使用者會把
  「現在有 3 個」當成全服只有 3 個。
- **移除「我的階級」選擇器**（Owner 2026-07-31）。它只在還沒解到【高難】時有作用，
  解開之後永遠不過濾任何東西，等於常駐一個沒用的控制項。
- **宇宙工具／基地進度分頁暫時隱藏**（Owner：「目前好像也沒什麼用」）。面板與資料都留著，
  拿掉 `hidden` 即可復原——不刪，因為工具升級鏈是原始需求之一。
- **連續任務改顯示整條鏈，並標明源頭是哪一類**。Owner 2026-07-31 指出「某些臨時任務的
  前置條件應該是完成普通任務」——資料完全吻合：88 個前置裡 **33 個指向 rank A 的基礎任務**、
  33 個指向【高難】、22 個指向【高難+】，鏈形是 `基礎 A →【高難】續·X →【高難+】X`。
  原本只寫「先完成『X』」，看不出 X 是哪一類、也看不出還有幾層 ⇒ 改成
  `先完成基礎任務「製作貨物包裝材料」→「【高難】續·製作貨物包裝材料」`。
  知道源頭在基礎分頁，才知道該從哪裡開始做。
- **16 個雙職業任務移出主面板**。Owner 實測：ET 12–14 且靈風時，本站列出「【高難+】製造高火力爐」
  （園藝＋甲冑）但遊戲內沒有，同時刻的兩個單職業任務都在。先猜「第二職沒練」，**被 Owner 直接否證**
  （兩個職業都有，仍看不到）。資料上它們自成一區：`JobCategorySecondary` 非零者恰好等於
  `c21 ∈ {20000, 20100}`（交集 16/16、id 連續 #496–511），且是全表唯一沒有 rank-4 對應版本的一組。
  出現規則未知 ⇒ **不放進主清單假裝算得出來**，只在面板底部說明有這 16 筆。記 B-013。
- **緊急事件告誡：只在特殊天氣進行中才顯示**。Owner 實測：靈風進行中突然轉孢子霧，
  限時任務整批消失、改出緊急事件。緊急天氣會**覆蓋**原本的天候，而那三種天氣不在任何
  機率表裡＝伺服器推播，本站既無法預測也無法偵測 ⇒ 這段時間上面的天氣限定任務全部不準。
  晴朗時本來就沒有天氣限定任務，常駐這段只會變成沒人讀的背景雜訊 ⇒ 條件顯示。記 B-014。
- **條件欄改顯示實際的值**（`ET 12:00–14:00`／`天氣：靈風`／`ET 00:00–02:00 或 ET 22:00–24:00`），
  不再只寫「時間限定任務」這種類別名——類別名看了還是得再點進去才知道幾點。
- **「下一批」改算「所有條件真正同時成立的下一刻」**。原本只看時段、忽略天候，於是會列出
  下一段天氣根本不對的任務——天候是純時間函數、算得出來，列一個不可能發生的東西沒有價值
  （Owner 2026-07-31）。改用 350 秒（＝2 艾歐澤亞小時）為步長向前掃：ET 視窗邊界正好是它的倍數、
  天候週期 1400 秒是它的 4 倍 ⇒ 涵蓋兩種條件的所有狀態變化，不會跳過開窗。掃描上限 4 天，
  結果依 350 秒區塊快取（每秒重畫但每 350 秒才重算）。順帶修掉先前「下一批 — 0 秒後」的 bug
  （取到還在進行中的天候視窗）。
- **獎勵拆成逐格對齊的欄**（宇宙／月球／Ⅰ Ⅱ Ⅲ Ⅳ）。擠成一串 `30/33　Ⅳ23 Ⅲ15 Ⅱ15`
  沒辦法上下比大小，而「哪個任務比較划算」正是看這欄的唯一理由。
  **欄數由資料推**（台服目前只到 Ⅳ），標題與資料列吃同一份清單 ⇒ 不會錯位。
- **有條件的基礎任務進看板**。舊的 `class !== 'basic'` 把它們全濾掉，於是 ET 16:00–18:00
  唯一開著的「製作貨物包裝材料」（基礎/A/皮革師）在看板上完全不存在。
  排除的改成只有「不限時的基礎任務」302 個。
- **主畫面砍掉重做成兩段清單**（`job-board-view.js` → `now-panel.js`）。
  職業看板一張卡片塞了必定／抽選／連續三個數字＋倒數＋緊急註記，看的人得先讀懂四個概念
  才知道要不要去玩（Owner 2026-07-31：「不要那麼複雜了」）。現在只有：
  「現在有 N 個限時任務」＋逐列（職業圖示｜條件種類｜任務名｜需先完成前置），
  以及「下一批 — M 後」的同格式清單。只列有時段／天候條件的 152 個，
  不限時的 392 個隨時都在、去任務清單分頁看。同時刪掉四道門的說明牆與抽選的定調段落。
  過程中修掉的一致性 bug：看板把「有前置的連續任務」算進抽選池、清單卻正確排除，
  於是卡片寫「抽選 9」點進去只有 6 筆。
- **子標籤只掛臨時任務**（Owner 依遊戲 UI 指正）。基礎與緊急即使有時段／天候條件也不掛，
  條件本身照樣列在「開放條件」欄。時間限定 113→96、天氣限定 52→41，連續 88 不變。
- **從看板點進清單時預設帶好篩選**：「只看條件成立的」預設開，並新增可見開關
  「隱藏不限時的基礎任務（302）」預設開——否則點任一職業進去，前幾十列全是 D 階的
  「在抽選池中」，卡片上算給你的那筆反而被埋掉。底部的基礎任務入口會把兩個開關都關掉。

### 這輪的教訓

四次把「我讀得到的欄位」當成「全部的條件」——緊急任務、階級門、第二個條件槽（c15）、前置鏈，
每次都是 Owner 在遊戲裡撞到才發現。這輪的根因是**沒去找遊戲自己的說法就自訂分類**：
Addon 表裡三個分頁名與三個子標籤一直都在，掃一次關鍵字就有。
往後對這批 sheet 的任何分類斷言，先問「遊戲的介面字串怎麼寫」，再談欄位反解。

## 2026-07-31 — 建站（cycle [2026-07-31-cosmic-site](docs/specs/2026-07-31-cosmic-site-design.md)）

### Added

- **`tools/cosmic-dump`（C#／Lumina）** — 從台服 client sqpack 產生 `data/weather.json`、`missions.json`、
  `cosmic-tools.json`。內建健全性閘（544 任務／天氣機率總和 100%／11 條 ×9 階工具鏈），**任一不過就整批不寫檔**
  ——分次落地會留下「舊檔／新檔／缺檔」三種狀態，而網站是逐檔讀的，等於靜默混用兩個 client 版本的資料。
- **三分頁靜態站** — 天氣時間軸／任務清單（544 筆，可依職業‧難度‧條件‧「現在符合條件」篩選）／宇宙工具升級鏈。
- DEVLOOP 工件、`AGENTS.md` 鐵則、portal 設計系統接線（accent cyan、`cos-` 私有前綴、`.codex-*` 零覆寫）。

### 為什麼是這個範圍

原始需求是「自動偵測各伺服器緊急任務與天氣任務的時間」。動工前先做離線查證，結論把範圍砍成兩半：

**一般天氣（月塵／晴朗／靈風）是 unix 時間的純函數**，全 7 個繁中服伺服器同步 ⇒ 可推算到任意未來，
而且「各伺服器」這個維度根本不存在。演算法正確性用中薩納蘭與摩杜納回推、與既有 sightseeing 的手打表逐項比對確認。

**緊急事件天氣（磁暴／流星雨／孢子霧，Weather#194–197）不出現在 client 全部 172 張 `WeatherRate` 的任何一列**
⇒ 時間演算法永遠擲不出來 ⇒ 只能由伺服器推播，離線零資訊。這是決定性證據，不是推測，所以本站**不提供也不假裝提供**，
改為給出唯一算得出來的線索：11 個緊急任務的必要條件視窗（靈風時段），並在頁面上把理由講明白。

### 刻意沒做的事

- **未定性欄位不填合理值**：`WKSMissionLotterySpecialCond` row 15–22 在台服 client 三欄全 0（上游國際服當它們是
  Clouds/Rain，但渴望灣天氣表根本沒這兩種天氣）⇒ 標 `unknown`、可接欄顯示「未知」。評價門檻與未實裝的工具階數同理。
  **由來是 ICE fork 那輪連續五次拿 0 當佔位**（`MapPosition` 讓採集完全不執行、`missionText` 讓技能完全不放）——
  0 在這批 sheet 裡是有意義的哨兵值；在網站上的同型錯誤形式是「把 6 個緊急任務標成隨時可接」。
- **欄位索引不複製第二份**：三張任務表的欄位索引由 csproj 直接編譯 ICE fork 的唯一定義。產生器本來就只能在
  有遊戲 client 的本機跑，依賴本機路徑可接受；抄一份的代價是台服改版後兩邊漂移，症狀是「數字合理但錯」。

### 順帶發現

- `ffxiv-tw-sightseeing/modules/weather.js` 的**月夜峰機率表是錯的**（手打表把 client 的「晴朗 70」拆成
  Fair 30 ＋ Clear 40）。已記 `docs/BACKLOG.md` B-001。
- portal 的 `.codex-empty` 設了 `display:flex` 但**不在** header.css 的集中 `[hidden]` 守衛名單內 ⇒
  帶 `hidden` 的空狀態照樣顯示。本站用自有 class `.cos-hideable[hidden]` 繞過，已記 B-002 提報 portal。
- `WKSMissionToDoEvalutionRefin`（544 列 × 3 欄）是 ICE fork `TcCapability` 仍停用的「銅星門檻」的候選——
  那邊掃了三張表沒找到，因為它在第四張。已記 B-005。

### 驗證

產生器健全性閘全過；瀏覽器 1920×1080 實測三分頁皆正常、console 零 error、水平溢出 0、
`.page-header` top=64／高 51px（生態基準值）；天候篩選實測 11 筆全為緊急且靈風時段內全標「可接」。
